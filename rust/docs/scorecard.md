# Endpoint Scorecard — Rust rewrite progress

Metric: `BASE_URL=http://localhost:<rust_port> bun tests/endpoint_test.js`
(the same suite that runs against bun). The rewrite is done when this is 100%.
Record one row per plan step; report honestly — partial passes are data.

| Step | Scope | Date | Pass | Total | Notes |
|------|-------|------|------|-------|-------|
| 1 | scaffold (no endpoints) | 2026-09-07 | 0 | ~80 | baseline pending Step 4 server |
| 2 | mail pipeline (mitch-mail + shims) | 2026-09-07 | n/a | n/a | see checks below — send path verified via parity + local TLS SMTP sink |
| 3 | ssh-gateway (russh + tokio-tungstenite) | 2026-09-08 | 13/13 | 13 | same suite passes against JS gateway too — parity proven |
| 4 | core skeleton (hosts, static, pipeline) | 2026-09-08 | 52/52 urls | 52+ | headers AND bodies byte-identical vs bun (104 checks) |
| 5 | data layer + crypto | 2026-09-08 | 8/8 paths | 8 | data parity harness: passthrough + reserialize byte-identical; 29 rust tests green |
| 6a | auth/sessions + rate limits | 2026-09-09 | 102/102 urls | 102+ | parity holds with the live rate limiter + real checkPasswordCookie; 6a test bypass order fixed |
| 7 batch 1 | misc read-heavy endpoints (site-info, bad-passwords, backgrounds, log-click, leaderboard, games) | 2026-09-09 | 102/102 urls | 102+ | six endpoints live with data layer + auth; parity unaffected |
| 7 batch 2 | captcha proxy, ping, content | 2026-09-09 | 102/102 urls | 102+ | worldshardestcaptcha proxy + ping + content endpoints live |
| 8 | admin route group (~60 endpoints) | 2026-09-13 | 102/102 urls | 102+ | parity_static green; admin gate probes byte-identical vs bun (401/403 ladder + passphrase-status passthrough) |

## Step 4 verification log (2026-09-08)

- `tests/parity_static.js` (52 URL cases x both servers, sequential): **headers AND response bodies byte-identical** across mitch.pro / rjuhsd.school / sexypickleclub.com — static assets, injected HTML pages (`/`, `/enroll/`, `/encrypt/`, `/preferences/`, `/index-sales.html`, `/team/`, `/faq/`), directory redirects (301/302), bell/blooket redirects, per-host manifests, 404s, protected files, CSRF-blocked POST, unified password gate.
- Discovery that simplified the port: **bun's static serving has no etag/Last-Modified/304/range support at all** — the contract is the mime map + three Cache-Control classes + COOP/COEP/CORP for `/webvm`.
- Bugs the parity harness caught and fixed, in order: viewport meta replacement dropped the tag's closing group; `safeWebrootPath` lacked `path.resolve`-style `..` normalization; `</head>` insertion sliced past the tag (JS inserts *before* it and keeps it); `jsonResp` built its headers but never attached them; the prelude's unified password gate and the rjuhsd-hub `/index.html` mapping were missing; SSO-bridge redirect targets needed `encodeURIComponent`.
- Session-dependent pieces stubbed unauthenticated (Step 6): `checkPasswordCookie`, bans, SSO bridge tokens, rate limits. The deferred `/images/*` captcha-proxy route is a later step (excluded from the test with a comment).
- The rewrite's progress logging flows into the shared `app_logs` table (category `rust-rewrite`) via `mitch-lib::log` + the `rust_log` example CLI; the admin panel log viewer was upgraded in the same pass (structured rows, level pills, click-to-expand details, debounced search, 10s live tail).
- Dev-note: this sandbox's Docker port-forwarding eats connections; parity runs used host processes (`bun server.js` on 6802 vs rust on 6801) with the dev container stopped to avoid double IMAP watchers, then restored.

## Step 2 verification log (2026-09-08)

- `cargo test --workspace`: 17 tests green (mitch-lib data layer 6, mitch-mail 11).
- `tools/mail_rs_parity.js`: **ALL PARITY CHECKS PASSED** — html/text/subject/from/headers byte-identical to the original JS `formatHtmlEmail` functions across gmail, gmail -a, gmail --raw, noreply, support, support --raw.
- Shim fallback: service unreachable → falls through to nodemailer (verified with dead MAIL_RS_URL).
- Full SMTP round trip: shim → mitch-mail → implicit-TLS SMTP sink delivered a real multipart/alternative MIME message with From/To/Subject/List-Unsubscribe/List-Unsubscribe-Post/Message-ID and both text + html parts.
- Live Gmail/Hostinger send: **pending** — no Doppler credentials on the dev machine; verify on the VPS (or wherever `doppler` is authenticated) before flipping prod traffic: `bun mail/send_email.js <self> "rust mail test" "body"` with the mail-rs service up.
- Deviations from JS, documented: `MAIL_SMTP_PORT` env override (default 465, JS hardcodes 465); lettre Message-ID format differs from nodemailer's but is RFC-5322-valid.

## Step 3 verification log (2026-09-08)

- `tests/ssh_gateway_test.js` (registered as `bun run test:gateway`, NOT in the CI pre-deploy chain): **13/13 checks pass against the Rust gateway** — invalid JSON, missing type, missing host/username, live-session double-connect rejection, unreachable host error, real-sshd full echo (connect → echo → PTY resize → exit → ws close).
- **Parity proven**: the identical suite passes against the original JS gateway (`bun ssh-gateway/server.js`).
- Test sshd: disposable container (alpine + openssh, root/mitch-e2e-pass, `docker run --network host … sshd -p 2222`) — host-network mode needed because the sandbox's docker userland proxy eats port-forwarded connections.
- Bugs the tests caught during the port, both fixed:
  1. the select loop treated russh housekeeping messages (`Success`/`WindowAdjusted` replies from `request_pty`/`request_shell`) as shell-close and tore the session down 1 ms after connect — only `None`/`Close` now close;
  2. the russh session `Handle` was dropped when `open_shell` returned, killing the driver task — it is now held for the connection lifetime and disconnected on teardown.
- Documented divergence: error-message TEXT for SSH-layer failures differs from ssh2's wording (e.g. "Connection refused (os error 111)"); protocol-level error strings ("Invalid JSON", "Missing message type", "Already connected", "host and username are required") are byte-identical.
- Compose: `ssh-gateway-rs` sidecar on 6821; cutover = flip `SSH_GATEWAY_URL` to it.

## Step 4 verification log (2026-09-08)

- `tests/parity_static.js` (52 URL cases × both servers, sequential): **headers AND response bodies byte-identical** across mitch.pro / rjuhsd.school / sexypickleclub.com — static assets, injected HTML pages (`/`, `/enroll/`, `/encrypt/`, `/preferences/`, `/index-sales.html`…), directory redirects (301/302), bell/blooket redirects, per-host manifests, 404s, protected files, CSRF-blocked POST, unified password gate.
- Discovery that simplified the port: **bun's static serving has no etag/Last-Modified/304/range support at all** — the contract is the mime map + three Cache-Control classes + COOP/COEP/CORP for `/webvm`.
- Bugs the parity harness caught and fixed, in order: viewport meta replacement dropped the tag's closing group; `safeWebrootPath` lacked `path.resolve`-style `..` normalization; `</head>` insertion sliced past the tag (JS inserts *before* it and keeps it); `jsonResp` built its headers but never attached them; the prelude's unified password gate and the rjuhsd-hub `/index.html` mapping were missing; SSO-bridge redirect targets needed `encodeURIComponent`.
- Session-dependent pieces stubbed unauthenticated (Step 6): `checkPasswordCookie`, bans, SSO bridge tokens, rate limits. The deferred `/images/*` captcha-proxy route is a later step (excluded from the test with a comment).
- The rewrite's progress logging flows into the shared `app_logs` table (category `rust-rewrite`) via `mitch-lib::log` + the `rust_log` example CLI; the admin panel log viewer was upgraded in the same pass (structured rows, level pills, click-to-expand details, debounced search, 10s live tail).
- Dev-note: this sandbox's Docker port-forwarding eats connections; parity runs used host processes (`bun server.js` on 6802 vs rust on 6801) with the dev container stopped to avoid double IMAP watchers, then restored.

## Static parity (tests/parity_static.js, added Step 4)

| Step | URLs checked | Diffs | Notes |
|------|--------------|-------|-------|
## Step 5 verification log (2026-09-08)

- `tests/data_parity.js` + `rust/crates/mitch-lib/examples/data_parity.rs`: **8/8 paths byte-identical** through both passthrough (stored TEXT verbatim) and reserialize (parse + js_stringify_pretty) modes — mixed shapes: strings, ints, floats (1.5, -2.75, 0.000001, 9007199254740992), nested objects, empty object/array, null, booleans, unicode, escaped quotes/backslashes/newlines, key ordering.
- `js_stringify_pretty`: JS JSON.stringify parity port — serde_json's default f64 formatting would emit `1.0` where JS emits `1`; the custom printer handles the JS Number.toString rules (decimal for 1e-6..1e21, exponential outside, `-0` as `0`).
- crypto.rs: `enc1:` seal/open ported (AES-256-GCM, key = HMAC-SHA256(ID_SECRET, purpose), purposes `dm-at-rest-v1`/`totp-at-rest-v1`); round-trip + RFC 4231 HMAC vector tests; ID_SECRET bootstrap (32 random bytes persisted on first boot).
- app_logs: append/query ported with the prune cadence (every 250th write, id-ordered, 20000 cap) — the admin log viewer's data source.
- Full table init parity: all core tables + indexes created on open with the SQLITE_BUSY retry ladder (10 retries, 100ms*attempt).
- Bugs the parity harness caught: the reserialize mode corrupted non-JSON string docs (JS writeDocument's string branch stores arbitrary text; JS readDocument returns the fallback on parse failure — the example now skips unparseable docs); the harness's string-doc expectation needed the same writeDocument ternary.

## Step 6a verification log (2026-09-09)

- `mitch-lib::auth`: normalizeEmail (reserved-locals domain folding), makeEmailId/validId (sha256[0..24] + HMAC-SHA256[0..16] with timing-safe compare), getCookies (legacy-cookie deletion + mitch_session re-derivation), authSessionFromToken (expiry + generation check + 5-min lastSeen refresh + names.json write-back), bannedInfoForEmail/Sid/Ip, issueLoginSession, cookie_path_attrs, checkPasswordCookie (full gate in JS order).
- Rate limiting: RATE_LIMITS table verbatim (~110 entries), sliding-window rlLog, anon = floor(max/5), detectNonHumanTiming (>=4 intervals within 10s, spread < 50ms), checkRateLimit with the polling-path exemptions, WHITELISTED_IPS.
- Wired into mitch-server: real check_password_cookie replaces the stub; get_real_ip (full header-precedence chain with the 172.16-31 bridge fallbacks); rate-limit gate in the prelude for /api/ paths.
- Parity: 102/102 headers+bodies byte-identical with the live rate limiter — the harness's ~51 sequential requests stay under the default [100, 60] bucket.
- Functional: hammering /api/stats on the rust server returns 429 after the bucket fills (verified with a 105-request curl loop).
- Test-semantics fix: the NODE_ENV=test bypass fires AFTER sid validation and email resolution (matching the JS order), not before — the test was asserting the wrong order.

## Step 8 verification log (2026-09-13)

- Admin route group ported as `routes/admin/{mod,dashboard,moderation,economy,legacy,data,vm}.rs` + a shared `AdminCtx` (cookies/sid/ip) — one file per concern, none over ~700 lines; `mitch-lib` gains `admin.rs` (passphrase store, admin-actions log, advanced-data builder, moderator panel/request engine) and `coins.rs` (multiplier, happy hour, gift notices, notifications).
- **Gate-order bug the parity probes caught**: bun's admin gate (server.js:7945) runs BEFORE the maintenance gate, the banned check, and password enforcement — the Rust port had it after the password prelude, so unauthenticated `/api/admin/*` returned `403 password required` instead of `401 unauthorized`. Moved the gate to prelude position 3c; the no-sid ladder (`passphrase-status` 403 passthrough, everything else 401, response bodies identical) now matches bun exactly.
- Admin role model corrected to the real admins.json shape (`{owners, admins, coOwners?}`, moderators a bare array, flat hierarchy); `is_admin_id` keeps the JS three-stage order (test backdoor → names.json generation binding → infinite token).
- Web-push fan-out ported with the `web-push` crate (VAPID from env, AES128GCM, 410/404 subscription cleanup); ntfy + bg-email stay fire-and-forget like the JS `proc.unref()` paths.
- Deferred stubs, documented: Proxmox executors + `ssh-key/copy-id` + `lxc-attach-sshd-hook` → Step 13 (russh) — they return structured failures and never write fake approval state; broadcast WS fan-out → Step 11; `/api/admin-members` + `/api/moderator-members` → Step 9 (need processMemberFields/profiles); ssh-key save re-encrypts via `ssh-keygen -p` instead of node PKCS8 (same russh-readable result).
- Gates: cargo fmt clean, clippy `-D warnings` 0 errors, `cargo test --workspace` 46+11+6 green; `tests/parity_static.js` 102/102 byte-identical.
- Merge adaptation (origin/master grew ~5000 lines: VM desktop portal, SSO fixes, games/matrix portals): synced the Rust copies of the password-exempt list (`/api/me/coins`, `/games`, `/matrix`, `/game-portal`, `/msn-games`, `/rjuhsd`, `/sexypickleclub`), the HTML auth gate (games/matrix open, ban page before the /enroll redirect, new `/admin/vms` actor gate), the static public gate, and the matrix SPA fallback; ported `GET /api/me/coins`. `tests/endpoint_test.js` now passes an identical 33 checks on both servers.

## Step 9 batch 2 verification log (2026-09-13)

- `routes/me/notifications.rs` ported: `/api/me/coin-gifts` (unread slice(0,10)), `/api/me/coin-gifts/read` (empty `ids` marks all; JS always writes `gifts[norm]` even when absent — mirrored), `/api/me/notif-prefs` GET+POST (bool keys via `=== true`, `HH:MM` regex incl. the 4-char `9:41` form, tzOffset `Number()` coercion + ±840 clamp, quietStart!==quietEnd when enabled), `/api/me/notifications` (4 families: admin_notice/coin_gift with `toLocaleString` amounts, DMs grouped per sender with `dmContentOf` + `[Secure Message]` e2e detection + 120-char cap + displayEmail, group DMs membership-gated with readBy check, matrix passthrough with `encodeURIComponent(roomId)` fallback), `/api/me/notifications/read` (marks coin_gifts + dms.read + groups.readBy(raw email) + matrix_notifications, `matrix:`/`matrix-` coinGiftIds prefixing, cancelPendingMatrixEmailAlert on AppState), `/api/me/complete-tutorial` (sid-only auth, ensureProfileDefaults + both tutorial flags, whole-profile replace).
- New shared modules: `mitch-lib::jsval` (JS coercion: truthiness, `String(v)`/`String(undefined)`, `||` fallbacks preserving raw truthy values, `Number()`, `toLocaleString` with en-US grouping + 3-fraction rounding) and `mitch-lib::chat` (`CHAT_EXPIRY_OPTIONS`, `dmExpiryKey`/`groupExpiryKey`, `getChatExpiry`, `isDmMessageRead`, `isMessageExpired` — read-gated auto-delete; shared with the Step 11 DM group).
- JS object-literal parity detail: absent matrix keys (`id`/`matrixRoomId`/`title`) are dropped by JSON.stringify — the port omits them rather than emitting `null`; `String(m.groupId || '')` vs `String(n.roomId)` distinguished accordingly.
- Auth detail preserved: every endpoint reads `cookies['studentId'] || cookies['id'] || ''` (the `auth_sid()` helper only checked the first).
- `AppState` gains `pickle_presence` + `matrix_pending_email_alerts` (cancel path live; delayed-alert scheduler → Step 11).
- Gates: fmt clean, clippy `-D warnings` clean, `cargo test --workspace` 53+11+6 green; `tests/parity_static.js` 102/102; `tests/endpoint_test.js` identical 33 pass / 71 fail on bun:6802 and rust:6803 (suite grew 90→104 checks with 14 new batch-2 cases; the me/* positive paths sit behind the prelude password/CSRF gates in both engines with byte-identical 403 bodies — deep positive-path coverage lands in Step 14b).

## Step 9 batch 3 verification log (2026-09-13)

- `routes/me/security.rs` ported: `/api/me/security-code` (6-digit codes, 10-min `PendingSecurityCode`, per-action labels) plus the shared security helpers — `verifySecurityActionCode` (expiry → 401, >5 attempts → 429, mismatch → 400), `verifyPasswordChangeSecondFactor` (totp-type → "invalid authenticator code" else the email-code path), `twoFactorConfig` (three truthy enabled keys, `twofa_type||twofaType||'email'`, at-rest-opened secret), `saveTwoFactorConfig` (reproducing the JS stale-outer-map spread: outer load first, `ensureProfileDefaults`'s own write second, then `{...p, ...stale, ...patch, updatedAt}`), and `makeVerificationCodeHtml` on the Step 8 dark shell. All four `/api/me/2fa/*` + security-code endpoints skip `validId` and resolve via raw sid, matching JS.
- `routes/me/account.rs` ported: `change-email` (lowercase+normalize, `passwords.json` collision check, 30-min `PendingEmailChange` token, "Email Change Request" mail), `change-email/confirm` (oldNorm binding, attempt ladder, then `renameEmailReferences` → invalidate old sessions → rotate generation → ntfy → fresh `authSuccessResponse`), `change-password` (async: `verifyRecaptcha` BEFORE the password-cookie gate, JS order for field/strength/match/argon2 checks, second-factor hook, accepts ANY method like JS), `logout-other` (rotate + fresh cookies), and `GET /api/me` (role ladder co-owner>owner>admin>moderator>member, banned 403 with reason fallback, maskEmail/displayEmail, blog contributor, premium, `vip_casino_until`, active cosmetics, e2e keys from `e2e_keys.json` else the P-256 legacy derivation with `history.slice(0,5)`, happyHour block from boot-computed `computedHappyHour`).
- Shared helpers: `authSuccessResponse` (create session + 4 Set-Cookie headers — `mitch_session` httpOnly, `studentId` not, clears password/id — values now `encodeURIComponent`-encoded via the new `auth::encode_uri_component`, fixing a `js_quote` quoting divergence before it shipped), `isSecurePassword` (UTF-16 length ≥8 + lowercase bad-passwords.json), `isBlogContributorEmail`/`canWriteBlogEmail` (array-or-object shapes), `renameEmailReferences` (17 key-map files with profiles.email update, names.json sid remap, tokens.json email/norm_email, friends move+remap, friend_requests from/to, recursive normalized-string rewrite across dms/public_chat/premium_chat/applications/sessions — `rebuildCoreTablesFromDocuments` stays deferred with the SQL sync).
- `routes/push.rs` gains the full `verifyRecaptcha` port (NODE_ENV=test → true; hardcoded `66.60.183.124`/loopback whitelist; per-sid 10-min success cache; RECAPTCHA_SECRET_KEY||SECRET_KEY; fail-open with no secret; google+recaptcha.net hosts with RECAPTCHA_VERIFY_URL override; invalid-input-response/bad-request → next host; RECAPTCHA_MIN_SCORE 0.3; fetch error fail-open; ONE 5s budget like the JS AbortController; success caches per-sid) — landed early so batch 4's friends/* can use it.
- **Bug found by the new JS-reference parity test**: `la_local_parts`'s wall-clock DST guess misclassified the post-transition PST hour of the November fall-back (09:00Z → 02:00 instead of ICU's 01:00). Rewrote `pacific_dst_active` to decide on the UTC timeline (spring = 2nd Sun Mar 10:00Z, fall = 1st Sun Nov 09:00Z); `la_parts_matches_js_reference` now pins 12 Intl-captured values including both transition instants and ±1ms.
- **`subtle` pitfall fixed in `e2e.rs`**: `CtOption::unwrap_or_else` evaluates its closure eagerly (constant-time select), so the `unreachable!` fallback fired even on `Some` — routed through `Option::from` whose `unwrap_or_else` is lazy. Temp probe test deleted.
- Gates: fmt clean, clippy `-D warnings` clean, `cargo test --workspace` 58+11+6 green; `tests/parity_static.js` 102/102; 17-case direct probe of every batch-3 endpoint (statuses, bodies, Set-Cookie headers) byte-identical bun:6802 ↔ rust:6803; `tests/endpoint_test.js` identical 43 pass / 71 fail on both (suite grew 104→114 with 10 new batch-3 cases, all passing; failing-set names diffed and identical across engines — deep positive-path coverage lands in Step 14b).

## Step 9 batch 4 verification log (2026-09-13)

- `routes/friends.rs` ported as its own route group: `/api/friends/request` (verifyRecaptcha gate with the "reCAPTCHA failed. Please try again." body, resolveTargetEmail → "user not found", self-request guard, already-friends guard, reverse-request auto-accept that splices the pending entry and writes both friend lists, duplicate-request guard, `{from, to, timestamp}` pending record), `request/cancel` (parse AFTER the session gates, "no pending request found to this user"), `request/respond` (accept/reject action ladder, friend-list union on accept), `remove` (filter both lists, save only on change, always `{ok:true}`), `list` (processMemberFields displayName, bio 120 slice, sanitizeProfileImageUrl allowData/120000, `/profile/?u=` handle URL, online/playing from the presence map), `requests/pending` (incoming/outgoing with maskedEmails + raw timestamps).
- New support: `push.rs::send_web_push_clean` (JS server.js:2263 — lookup by NORMALIZED email only, VAPID gate, awaited like the JS `await`, 410/404 delete + save; `send_web_push` now returns the gone flag so both callers own their deletion semantics); `AppState.user_presence` (`userPresence` map + `PRESENCE_FALLBACK_TTL_MS` 45s — the `/ws` heartbeat writer lands in Step 11, TTL reads are live).
- Gate detail preserved: friends flows order JS's parse-vs-auth differently per endpoint (request parses first; cancel/respond/remove authenticate first) — mirrored exactly.
- Gates: fmt clean, clippy `-D warnings` clean (one `result_large_err` fix: `session_email` returns `(u16, &'static str)`), `cargo test --workspace` 58+11+6 green; `tests/parity_static.js` 102/102; 12-case direct probe of every friends endpoint byte-identical bun:6802 ↔ rust:6803; `tests/endpoint_test.js` identical 50 pass / 71 fail on both (suite grew 114→122 with 7 new batch-4 cases, all passing; failing-set names diffed and identical).

## Step 9 batch 5 verification log (2026-09-13)

- `routes/pickle.rs` ported as its own route group — 16 endpoints over three JS regions: The Barrel (`/api/pickle-chat/send` with the chatroom_bans `pickle` key ladder — ban → 403, timeout with `Math.ceil` seconds message, expired-timeout cleanup write; last-10-consecutive spam check → `logCheat('chat_spam')`; shadow-ban → 200 with nothing stored; msg = `{id: hex8, name, email, text(1000 UTF-16 slice), ts, color, badge||null, chatEffect||null, reactions:{}}` with the color key OMITTED when `publicActiveColor` is undefined, then `history.slice(-1000)` save + `picklePresence.set`; react toggle with `[...emoji].length > 3` code-point guard and delete-on-empty emoji lists; presence heartbeat with 90s prune; history slice(-100) with `processMemberFields` + recomputed color (`{...msg, color: undefined}` drops the key when absent) + `pickleJarFor(members cache)`), Clubhouse (`vote` with `Number()` coercion where JSON null = 0 but an absent key = NaN → 400, 30-day lex-sort trim; `crunch` Math.round 1-10; `join` parsing NO body; `decide`/`owners` founder-gated with Jar-number allocation over founders(3)+roster+owners; `membership`/`applicants`/`features` GETs — applicants sort `statusRank ?? 3` then requestedAt desc (stable), features with the 32-bit `h*31+c` daySeed → POTD matchup, crunch average `Math.round(x*10)/10`, Top Briners stable sort + 🥇🥈🥉🥒, presence prune + first-seen-order online names, viewer badges incl. Original Brine from firstSeen order, `ensurePickleMember` beacon, membership founder/coowner flagging), Bulletin (`post` with `\s+` collapse + 140 title slice + `sanitizeBlogHtml(body.html||body.text, text.slice(0,20000))` + unshift + `slice(-500)`; react/delete mirror the chat toggle; state GET pinned-then-ts sort with viewer-relative myReactions/mine).
- New shared code: `mitch-lib::blog` (`htmlEsc`, `safeBlogUrl` — images only allow absolute http(s)!, `legacyBlogHtmlFromText` split on `\n{2,}`, `sanitizeBlogHtml` — the JS combined pair-regex has no regex-crate backreference equivalent, so it is reproduced as one compiled pair-regex per banned tag preserving the lazy `<open …[\s\S]*?</close>` scan; attribute regexes keep href/src/alt with htmlEsc'd output), `admin::log_cheat` (cheat_logs.json unshift cap 1000), `jsval::js_slice_utf16` (JS `String.slice` UTF-16 code-unit semantics — emoji count 2; a cut landing mid-pair drops the whole pair since JSON can't carry lone surrogates), `school::utc_iso_day` (`new Date().toISOString().slice(0,10)` via civil-from-days, tested against JS incl. pre-epoch).
- `AppState.pickle_presence` switched from HashMap to an insertion-ordered `Vec<(String, i64)>` — JS `Map.set` keeps position on update, and the features endpoint's `online.names` iterates in first-seen order.
- All the `isBroadcast` WS fan-outs (pickle_chat / pickle_chat_react / pickle_bulletin_new / pickle_bulletin_react / pickle_bulletin_delete) are deferred to the Step 11 WebSocket work — every HTTP response is complete.
- Gates: fmt clean, clippy `-D warnings` clean, `cargo test --workspace` 60+11+8 green (new: blog sanitizer vs 15 bun-golden cases incl. `<script>x</div>y</script>` pair-extension and relative-img-src drop; POTD daySeed 1468146467 → (11, 4) vs bun; presence Map.set order); `tests/parity_static.js` 102/102; 21-case direct probe of every pickle endpoint + wrong-method fall-through byte-identical bun:6802 ↔ rust:6803; `tests/endpoint_test.js` identical 67 pass / 71 fail on both (suite grew 122→138 with 16 new batch-5 cases; failing-set names diffed and identical).

## Step 9 batch 6 verification log (2026-09-13)

- `routes/members.rs` ported — userdata + member rosters (5 endpoints): `/api/userdata` (the JS branch checks the path only, so EVERY method lands in it — the GET-specific branch at server.js:19048 is unreachable dead code; `typeof body !== 'object'` passes for `null` (JSON null proceeds, `Object.assign(null, …)` throws → 500 — mirrored; arrays rejected 400), sha256(sid).hex[0..32] dir key under `/opt/userdata`, merge keeps existing-key positions / appends new keys in source order, `_updated` = seconds float, 1 GiB quota → 413 `quota_exceeded`, 503 `Storage unavailable` when `/opt/userdata` can't be created (root-owned 755 in dev), `syncNewsletterUnsubscribe` ladder (strict `=== false` add / strict `=== true` remove, dedupe-then-sort, JSON-parse failure swallowed by the outer catch)); `/api/members` (member gate → 401 `auth required`, skip revoked/TEST_ACCOUNT/non-admin, role ladder owner>developer>admin>moderator>contributor>premium>member, e2e pubKeyHex-or-legacy with `e2eReady` truthy flag, DM filter with NaN-skip via `partial_cmp`, unread = to==viewer && !read, lastMessage winner by ts with JS spread key-keep semantics, sort ts desc → role_rank desc → online desc → email asc); `/api/admin-members` + `/api/moderator-members` + `/api/owner-members` rosters with the exact exclusion/normalize/dedupe ladders.
- Shared helpers: `me::dm_content_parts` extracted (returns `(Option,Option)` where `None` = JS `undefined`); `notifications::dm_content_of` now delegates — **this fixed a real divergence**: `openAtRest` returns the ORIGINAL sealed string on any failure (server.js:465), so a corrupt at-rest row must yield the raw sealed text, which the old Rust helper didn't do; `friends::{is_user_present, encode_uri_component}` made `pub(crate)`.
- Dev-baseline limitation (documented, not a gap): in dev there is no `auth_sessions.json`, so every endpoint here returns the global 403 `password required` (anon and valid-sid) or 403 `csrf_blocked` (no-Origin mutations) on BOTH engines — the batch-5 methodology again. Origin'd non-GET parity is deferred to Step 14b: handler.rs:281's Step-4 stub 405s all non-GET after CSRF passes, so Origin'd POST/PUT/DELETE can never be parity-tested in dev until the stub is lifted. Deep positive-path logic is covered by unit tests instead: userdata dir-key golden (sid `e8a9…7972` → `7e523c1316648cffdc2eb7a03d69e1ec`, matches bun), Object.assign key-position merge, newsletter-unsub strict-bool add/remove ladder (DB-routed document via `DataStore`), and the members sort ladder.
- Gates: fmt clean, clippy `-D warnings` clean, `cargo test --workspace` green (one flaky pre-existing failure in untouched mitch-lib `log::tests::message_length_capped` — SQLITE SystemIoFailure 1802 — passes on most runs and standalone, unrelated to this batch); `tests/parity_static.js` 102/102; 14-case direct probe of every batch-6 endpoint byte-identical bun:6802 ↔ rust:6803; `tests/endpoint_test.js` identical 75 pass / 71 fail on both (suite grew 138→146 with 8 new batch-6 cases, all passing; failing-set names diffed and identical).

## Step 10 batch 1 verification log (2026-09-14)

- Canvas state core ported as `routes/canvas/` (dir module replacing the stub): `canvasPixels`/`canvasChunks` as in-memory order-preserving maps loaded at boot (server.js:4312-4315 + `rebuildCanvasChunks`), the 30s `saveCanvasPixels` flush and the hourly `canvasHeatmap` 24h sweep now REAL tokio tasks in `workers.rs` (first live interval workers — the file was a stub). Persistence parity: `canvas_pixels/locks/banned/reports/bookmarks/zones(+zone_pixels_*)` go through the SQLite-backed `read_document`/`write_document` layer like JS `loadJson`/`saveJson`, but the `*_history_*.jsonl` files use raw `std::fs` append/read because JS uses `appendFileSync`/`readFileSync` (server.js:4492, 21728) — they never touch the DB on either engine. serde_json gotcha discovered: this serde_json version only implements `entry`/`get_mut` on `Map<String, Value>`, so chunk maps hold `Value::Object` inners — which is also literally what the JS `if (!has(ck)) set(ck, {})` stores. NaN chunk keys preserved ("NaN,NaN" chunk entries from non-numeric coordinates, filtered from bounds by `Number.isFinite`), heatmap is a JS-Map-shaped insertion-ordered Vec (position kept on update; sweep keeps entries at EXACTLY 24h since JS deletes only `now - ts > 86400000`).
- Read endpoints ported in `routes/canvas/reads.rs`: `/api/canvas/chunks` (path-only method check — every method lands in it; bounds min/max over `Number.isFinite` coords, 0,0,0,0 when empty), `/api/canvas/pixels` (GET query + POST body forms; `zoneId` kept untyped so a truthy non-string still takes the zone path and 403s in `checkZoneAccess`; chunk selection = `String(chunksParam||'').split(';')` or array-as-Map-keys where non-strings miss; `Object.assign` merge order preserved), `/api/canvas/history` (raw-fs JSONL, last 5000 lines, malformed line aborts with entries-so-far kept — JS single-try semantics), `/api/canvas/whoami` (isAdmin/isModerator/email-null/isPremium), `/api/canvas/heatmap` (GET-only — it sits in the JS GET-routes region), `/api/canvas/admin-bans` (fresh `loadJson` per call), plus the disabled stubs `POST /api/canvas/moderate` (200 ok/flagged:false/disabled:true) and `POST /api/canvas/report` (410). `checkZoneAccess` (admin → owner → friendsOnly both-directions → allowedUsers) ported for the zone branches. In-handler `checkRateLimit` calls stay no-ops (JS prelude stamps `req._rateLimitChecked`); the Rust prelude covers it.
- JS dead code noted, not ported: `detectCanvasIssues`/`addCanvasReport`/`swastikaTemplates` have zero call sites in server.js. `broadcastCanvasDelta` fan-outs + the WS canvas channel land with Step 11. Origin'd non-GET parity stays deferred to Step 14b (handler.rs:281 405 stub) — POST /api/canvas/pixels is therefore gate-tested only in dev.
- Gates: fmt clean, clippy `-D warnings` clean, `cargo test --workspace` 60+11+15 green (new: chunk-key floor-division quadrants incl. NaN chunk names, rebuild skip of comma-less keys, heatmap insertion-order + exact-24h sweep boundary); `tests/parity_static.js` 102/102; 20-case canvas probe byte-identical bun:6802 ↔ rust:6803 (reads anon+auth, zoneId paths, no-Origin POST stubs, PUT fall-through); `tests/endpoint_test.js` identical 83 pass / 71 fail on both (suite grew 146→154 with 8 new canvas cases, all passing; failing-set names diffed and identical).

## Step 10 batch 2 verification log (2026-09-14)

- Painting family ported as `routes/canvas/paint.rs` (6 endpoints): `POST /api/canvas/pixel` (the full JS guard ladder in order — zone access → brush cap (`(adminOk||zoneId)?50:premium?16:8`) → missing fields (`typeof x/y === 'number'`, NO string coercion) → color regex → bounds (NaN passes, exactly like `Math.abs(NaN) > 500000` being false) → `canvasBanned` (reason omitted from the response when falsy, like JSON.stringify dropping undefined) → premium color; brush loop `ceil(brushSz)` iterations for fractional brushSz; lock TTL + premium-own-pixel skip rules; shadow-ban shadow-success AFTER the loop but BEFORE any paint; `pixelData` key order color,painter,ts,email?,premium?,admin?; weekly lock budget (7-day `last_lock_reset` reset, 16/week cap, `expiresAt = now+86400000`); `addPaintingCoin` fires even when zero pixels land); `POST /api/canvas/pixels/bulk` (premium/admin gate, `slice(0,2500)`, `Number.isInteger` on coerced `Number(p?.x)`, `painter` = `String(p?.painter || email || 'admin').slice(0,120)` UTF-16, and the DIFFERENT `pixelData` key order color,painter,ts,admin?,premium?,email? — admin BEFORE premium, unlike the single-pixel endpoint); `POST /api/canvas/erase` (missing-fields BEFORE brushSz — opposite of pixel POST; `isOwner` from zones.json owner, `isCreator` fallback, no banned/coin checks); `POST /api/canvas/admin-erase` (raw `${x},${y}` template keys — x/y can be strings, so a new `delete_pixel_raw` keys the pixel map off the template while the chunk key comes from `Number(x)`, and the history line keeps the RAW x/y values with JS-undefined keys dropped); `POST /api/canvas/admin-ban` (painter-email lookup over live pixels, `admin@mitch.pro` protection, `saveCanvasBans` updates the in-memory `canvasBanned` AND the file — new `CanvasState.bans` field; `painter.slice(0,12)` UTF-16) and admin-unban (404 not banned).
- New `mitch-lib::achievements`: `ACHIEVEMENT_DEFINITIONS` (22 entries, source order), `update_stat` (`|| 0` merge + award walk), `award_achievement` (idempotent, bonus through `addCoins`), `add_painting_coin` (UTC-day reset ladder, 1000/day cap with the JS re-load semantics, 0.2 coin, `pixels` stat bumped unconditionally even on capped days).
- **Fixed a real Step-8 parity bug the new tests caught**: `coins.rs`'s `lifetime_earned` bump REPLACED the user's whole `user_stats.json` entry with `{"lifetime_earned": …}`, wiping pixels/streaks on every coin award. JS merges (`stats[norm].lifetime_earned = …`, server.js:3092-3095) — Rust now merges too.
- `js_num_to_string` upgraded to full ECMA-262 Number::toString: shortest digits via `{:e}`, exponential when the ECMA decimal exponent n ≤ -6 or n > 21 (`1e-7` → "1e-7", `1e21` → "1e+21"), `String(-0)` = "0" — the old `as i64` truncation that would have broken fractional pixel keys is gone; history lines now write integral coordinates as integers (`5` not `5.0`, matching JSON.stringify).
- Dead code not ported (documented): the `checkRateLimit`-gated bypass-cooldown branches in pixel POST are unreachable in JS (prelude stamps `req._rateLimitChecked`); `broadcastCanvasDelta` fan-outs land with Step 11.
- Gates: fmt clean, clippy `-D warnings` clean, `cargo test --workspace` 62+11+16 green (new: achievements award-once ladder + painting coin day cap incl. the empty-email no-op; pixel_key ECMA number-to-string boundaries incl. fractional/±0/1e-6→1e-7 and 1e20→1e21); `tests/parity_static.js` 102/102; 32-case canvas probe byte-identical bun:6802 ↔ rust:6803 (batch 1's 20 + 12 new: no-Origin POSTs for all six endpoints incl. bad-json 400, GET fall-throughs for POST-only paths, auth'd painting bodies); `tests/endpoint_test.js` identical 90 pass / 71 fail on both (suite grew 154→162 with 8 new cases, all passing; failing-set names diffed and identical).

## Step 10 batch 3 verification log (2026-09-14)

- Bookmarks + zones + admin report-status ported as `routes/canvas/zones.rs` (11 endpoints): bookmarks POST (`typeof x/y === 'number'` + `!name` gate → 400; `todayStr` = UTC ISO day; the userToday RAW `b.creator === email` compare; public ≥ 1 → 429 "You can only create 1 public bookmark per day." / private ≥ 10 → 429 "You can only create 10 private bookmarks per day."; id = `'bm_' + Date.now().toString(36) + Math.random().toString(36).slice(2,6)`; `approved: !isPublic`); bookmarks GET (email gate → 401 `Unauthorized` BEFORE admin check; admin all / creator raw == / isPublic && approved); bookmarks/approve (admin → 403 forbidden, falsy id → 400 missing id, 404 bookmark not found); zones POST (email gate → 401, `verifyRecaptcha(body.recaptcha_token||'', ip)` → 400 "reCAPTCHA failed. Please try again." with the sid arg empty like the JS 2-arg call; 10-zone cap counts `normalizeEmail(z.owner) === normEmail`; zone doc `{id, name slice60, description slice120, owner, friendsOnly !!, allowedUsers: [], createdAt}`); zones GET (reuses `check_zone_access` — the exact port of the same admin → owner → friendsOnly both-directions → allowedUsers ladder, preserving Object.values order); zones/add-user (owner-or-admin gate; `resolveTargetEmail` None → 404 "User not found" — JS's try/catch means a missing `allowedUsers` array ALSO 404s via the TypeError catch, mirrored; already-added normalized → 400 "User already added"; pushes the RAW resolved email); zones/remove-user (findIndex normalized → splice → 200 with the updated allowedUsers); zones/delete (`delete zones[zoneId]`; rmSync's the RAW zone_pixels_/zone_history_ files only — DB docs untouched, JS is identical; the in-memory zone caches are NOT cleared, JS doesn't either); zones/update (`!== undefined` key-presence guards so an explicit null still applies with `String(null||'')` = ''); zones/clear (removes the zone_pixels/zone_chunks cache entries, rewrites the pixel file `{}` through the DB layer, truncates the raw history file, broadcast deferred to Step 11); `/api/admin/canvas-report-status` (admin gate → 403; `String(body.id||'')` + `String(body.status||'').slice(0,40)`; whitelist Needs review/Reviewing/Resolved/Dismissed → 400 invalid status; 404 "report found"; reviewedAt/reviewedBy + `logAdminAction(reviewedBy, 'canvas_report_status', {id, status})`).
- New helpers: `to_base36(u64)` for `Date.now().toString(36)` (golden-tested against bun: 1736900000000 → "m5x5exa8", 2^32 → "1z141z4", 2^53−1 → "2gosa7pa2gv"); `random_base36_4()` for the Math.random id suffix (the JS shortest-digit form can rarely emit fewer than 4 chars — ids are random either way, so the always-4 shape is the contract, documented in the module header); `norm_js` keeps the JS `normalizeEmail(v)` falsy gate (`null/0/false → ''` BEFORE the String coercion, and `String({})` lowercases to "[object object]"); `paint::admin_ok` made `pub(crate)` for reuse.
- `/api/admin/canvas-report-status` lives in the canvas module — the admin group's unmatched `/api/admin/*` paths fall through to it, exactly like the JS block ordering (admin_gate still fires early from handler.rs, dev → 403 password required for GETs / csrf_blocked for no-Origin POSTs, verified on both engines).
- Dev-baseline limitation (per the batch-2 methodology): every endpoint here returns the global 403 `password required` (anon AND valid-sid) or 403 `csrf_blocked` (no-Origin mutations) on BOTH engines; Origin'd non-GET parity is deferred to Step 14b (handler.rs:281 405 stub). Positive-path logic is unit-tested instead: base36 goldens, id shape/charset, and the normalizeEmail falsy-gate ladder incl. the `[object object]` case.
- Gates: fmt clean, clippy `-D warnings` clean (after replacing the Result<_, Response> gate helpers with bool/Option — clippy result_large_err), `cargo test --workspace` 62+11+20 green (new: to_base36 bun goldens, 64× random-id-suffix shape, bm_/zone_ id shape, norm_js/string_or_empty semantics); `tests/parity_static.js` 102/102; 48-case canvas probe byte-identical bun:6802 ↔ rust:6803 (batches 1-2's 32 + 16 new: bookmarks/zones anon+auth GETs, no-Origin POSTs for all 11 endpoints incl. bad-json 400, GET/PUT fall-throughs); `tests/endpoint_test.js` identical 101 pass / 71 fail on both (suite grew 162→172 with 10 new batch-3 cases, all passing; failing-set names diffed and identical).

## Step 11 batch 1 verification log (2026-09-14)

- DM shared layer ported as `mitch-lib/src/dm.rs`: `dmStoreFiles` (DMS_MAIN/DMS_PICKLE with the `pickle_*` file sets), the body caps (`MAX_JSON_BODY_BYTES` 256KiB default, `MAX_CHAT_JSON_BODY_BYTES` = min(4MiB, max(base, env||2300KiB)), `MAX_E2E_ENVELOPE_BYTES` = min(chat−4096, 2200KiB) — all `Number(env || default)` falsy-gated), `CHAT_EXPIRY_OPTIONS` + `dmExpiryKey` (sorted norms joined `|`) + `groupExpiryKey` + `getChatExpiry` (the four validated windows, NaN→0), `isDmMessageRead` (group: readBy contains any reader ≠ sender; dm: `read === true || truthy(readAt)`), `getExpiryForMsg` (per-call chat_expiry.json read with the `spc:` host prefix), `isMessageExpired` (auto-delete is READ-gated; expiry>0 deletes on hour-old OR older-than-expiry OR past-expiresAt OR read-for-expiry; else only past-expiresAt), `pruneDms` (newest-first walk, drop expired, keep newest 100 per conversation keyed `g::<gid>` / `d::<sorted norms joined ::>`, reverse back), `dmAddressIndex` (profiles.json emails+usernames+default usernames, names.json values, tokens.json values.email; 15s cache) + `resolveMemberRef` (the exact ladder: `@` → emails set → byMask → local-part-dotted username fallback → norm; non-email → byUsername verbatim), `isHex(value, exactLen)` (JS `!exactLength` skips the equality check; even-length ASCII hexdigits), `validCipherPayload` (iv hex24, ciphertext any even hex, recipientPubKey optional 130-hex `04…`), and `validateE2eEnvelope` (byte-cap 413 → parse/object/e2e===true 400s → `envelope.version || 1` FALSY-coalesce so version 0 is valid → 1..=3 integer → senderPubKey → `isGroup !== groupExpected` mismatch → group payloads 1..=64 with per-recipient validation / 1:1 payload). 5 unit tests incl. bun-golden expiry keys and the resolveMemberRef subtleties (`a.li+ce` non-email → verbatim; the dotted email case resolves because normalizeEmail itself strips dots/plus).
- `/api/dm/send` ported as `routes/dm/` (dir module: `mod.rs` dispatcher, `send.rs`, `notif.rs`), wired into handler.rs BEFORE canvas (JS file order: dm 19452 < canvas 21634). Full ladder: rate limit (`getIdKey` from the same cookies) → `sid = studentId || id` → valid+not-revoked → 401 `auth required` → `names[sid]` lowercase empty → 403 `email not found` → pickle host swap (DMS_PICKLE + `spc:` expiry prefix + sexypickleclub.com cellar deep links) → Content-Length declared > chat cap → 413 `message request too large` BEFORE parse (garbage header = NaN, NaN > cap false) → oversized/invalid body → 400 `bad json` → `String(body.groupId)` truthy-gate → `resolveMemberRef(body.to||'') || normalizeEmail(...)` → image ladder ({url+id} passthrough with id64/url250/name180/size/mime80 slices, or data-URL: mime regex → `data:<mime>;base64,` prefix → 900KiB byte cap → 413) → E2E envelope detection (JSON-parse of trimmed text with `e2e === true`) + full validateE2eEnvelope → text slice (500 with image, else 2000, UTF-16) → missing-fields 400s → clientId idempotency (char-filter + slice120; reverse find matching clientId + not expired + same sender + group-kind/groupId or dm-kind/to-norm → 200 `{success, duplicate:true, message:{...existing, text, image, from, to?, readBy}}` with display-mapped emails and the JS undefined-drops: `to` omitted when `existing.to` falsy, `image` key only when present) → expiry (requested whitelisted against the four windows; conversation-wide `getChatExpiry(dm:…|…)` wins over per-send) → group branch (find by STRICT `g.id === String(groupId)`; membership via resolveMemberRef||norm → 403 `not a member`; msg key order kind,groupId,groupName?,from,text,image,replyTo,ts,readBy with missing group.name dropping the key) / dm branch (kind,from,to,text,image,replyTo,ts,read:false) → sealAtRest({text, image}) overwrites text + sets image:null (key order preserved by insert) → clientId/autoDelete appended → pruneDms → save → display-name ladder (displayName||nickname||username||canonical local-part) → notification fan-out AFTER save (JS order) → `addCoins(senderEmail, 2.0)` with the global multiplier → maskedMsg (unsealed via dm_content_parts, display-mapped from/to/readBy, `to` omitted for group messages like JSON.stringify dropping undefined) → 200 `{success:true, message}`.
- New notification helpers in `dm/notif.rs` (the per-user-topic family, distinct from push.rs's env-topic helper): `loadNtfyTopics` (ntfy_topics.json), `notificationUrl` (leading-slash normalize), `notifQuietNow` (HH:MM regex parse, tzOffset clamp ±840 Math.round, local-minutes shift with the JS Date convention, wrap-around windows), `notifAllowed` (`p[key] === false` blocks, else not-quiet — only an EXPLICIT false blocks), `ntfyNotifyUser` (topic regex `^[a-zA-Z0-9_-]{6,64}$`, Click header absolutized against site.json primary origin with new URL failure → relative kept, Title slice120 + Priority default + Tags lock, body slice400, fire-and-forget spawn like the silent JS catch). `loadPushSubscriptions` ported (normalized keys, truthy-object filter). `me::notifications` made `pub(crate) mod` for `get_notif_prefs` reuse.
- Known Step-11-batch-1 divergences (documented, land with the `/ws` batch): the WebSocket `new_dm` broadcast is deferred (HTTP responses are already full parity), and `recActive` (from the in-memory `e2eUsers` E2E-presence map fed by /api/e2e/*) is always false — it only gates WHICH notification channel fans out, invisible in HTTP responses. The JS `msg.readBy || []` non-array TypeError (500) degrades to an empty list in Rust (corrupt-data-only path). `/api/dm/send` has NO method check in the JS — any verb reaches the ladder; Rust mirrors.
- Dev-baseline limitation (per the batch methodology): valid-sid requests hit the global 403 `password required` gate and no-Origin POSTs hit 403 `csrf_blocked` on BOTH engines, so the deep ladder (send/dup/seal/notifications) is unit-test-verified only; Origin'd non-GET parity is deferred to Step 14b (handler.rs:281 405 stub — Origin'd POSTs would WRITE on bun, so the probe never sends them). Probe depth verified: anon AND valid-sid GET /api/dm/send → 403 `password required` (the global gate fires before the dm ladder), byte-identical on both engines.
- Gates: fmt clean, clippy `-D warnings` clean (after Result<_,Response>→(u16,&str) for validate_image, same result_large_err lesson as batch 3); `cargo test --workspace` 67+11+20 green (new: expiry-key goldens, four-window validation, prune 100-cap + read-gated expiry, resolveMemberRef ladder, E2E envelope ladder incl. version-0-valid + bad sender key + target mismatch); `tests/parity_static.js` 102/102; 55-case probe byte-identical bun:6802 ↔ rust:6803 (canvas batches' 48 + 7 new: dm/send anon/auth GETs, no-Origin POSTs incl. auth'd body + bad json + 300KB oversized, unknown /api/dm/ path fall-through); `tests/endpoint_test.js` identical 104 pass / 71 fail on both (suite grew 172→175 with 3 new batch-1 cases, all passing; failing-set names diffed and identical).

## Step 11 batch 2 verification log (2026-09-14)

- The remaining six chat-management endpoints ported as `routes/dm/inbox.rs` (groups GET + inbox) and `routes/dm/manage.rs` (mark-read, groups POST, group/leave, clear, expiry, report), with the shared `dm/mod.rs` gaining the dispatch (now `search: &str` for the query family, mirroring the canvas::handle signature), the auth ladder (`dm_auth` → 401 `auth required` / 403 `email not found`), and the JS-coercion helpers the batch leans on: `norm_of` (normalizeEmail's falsy gate BEFORE String(v)), `member_norm` (resolveMemberRef(m) || normalizeEmail(m)), `js_eq` (=== on JSON values: numeric equality across int/float reprs, identity for objects/arrays — never equal — and strict type mismatch otherwise), `concat_key` ('group:' + g.id with missing → "undefined" / null → "null"), `cleared_lookup` (myCleared[k] || 0 in a || chain: Some only when TRUTHY — an unparseable truthy value is NaN, which is falsy in JS and must keep falling through), `read_by_some` (normalizeEmail-folded readBy containment; non-array degrades to not-found where JS would 500), `js_num_value` (moved up from send.rs) and `string_prop` (String(m.groupId) with a missing key rendering "undefined").
- **GET /api/dm/groups**: groups filtered by member_norm membership; per group the clearedAt gate (`myCleared['group:'+g.id] || 0`), ts-desc stable sort, unread count, `lastText` (raw truthy text value kept — JS `text || …`; image truthy → 'Sent an image'), `lastMessage` ({...last, text, image (dm_content_parts fallback already resolved), from, to ONLY when truthy (undefined-drop), readBy display-mapped, non-array → []}), `lastTs: last ? last.ts : g.createdAt` (a present-but-null ts is KEPT as JSON null; an absent one drops the key — `filter(truthy)` would have been wrong), members as public username refs (profile username || defaultUsernameForEmail; unresolvable → displayEmail), createdBy display-mapped, `lastTs`-desc stable final sort. `publicRef` is NOT masked emails because the client encrypts one group payload per member keyed by this ref.
- **GET/any-verb /api/dm/inbox**: NO method check in the JS. `with` lowercased (no trim), resolveMemberRef → canonical with verbatim fallback; since/before/limit via `parseInt(qs.get(x)||'0')||0` (decimal prefix parse — "12.9" → 12, unlike Number). Filter ladder: isMessageExpired → since → `?group=` branch (kind 'group' + String(m.groupId) === groupId + `myGroupIds.has(groupId)` as a Set of RAW ids (SameValueZero — a numeric m.groupId never matches a string id) + clearedAt) → `?with=` branch (kind !== 'group' verbatim; clearedAt chain `['dm:'+withNormEmail] || ['dm:'+normalizeEmail(withUser)] || (withUsername ? ['dm:'+withUsername] : 0)`; isMeRef/isPeerRef — username + masked + display-email ladders, masked compared RAW for self (`nv === myMaskedEmail`) but NORMALIZED for the peer (`nv === normalizeEmail(withMaskedEmail)` — the JS is deliberately asymmetric and both sides are replicated) → general branch (group msgs via raw-id Set + concat-key clearedAt; DMs via peer resolution from the isMeRef(m.from) ternary and the 3-key cleared chain). Chronological stable sort → before filter → slice(-limit) → map {...m, text, image, from, to (UNGATED — group messages yield "to": "" like displayEmail(undefined)), readBy}. `expiry` from getChatExpiry(expPfx + groupExpiryKey/dmExpiryKey); `undefined` (no `with`) drops the key from the JSON. myEmail masked.
- **POST /api/dm/mark-read**: auth WITHOUT the revoked check and WITHOUT the email gate (myEmail may be '' — a falsy readBy entry then matches normalizeEmail('')===''; replicated). `tryParseJson()` failure → 400 with an EMPTY `{}` body (only mark-read does this — the other five return `{error:'bad json'}`); an EMPTY body parses to `{}` SUCCESS in JS (body = raw ? JSON.parse : {}) — mirrored by parse_body_strict. Group branch: kind 'group' + STRICT m.groupId === String(groupId) + !readBy.some(norm) → readBy push + readAt || now. DM branch: isFromPeer/isToMe ladders (fromUsername's third fallback is the raw `from` — even with an '@' — while mark-read's myU has no fallback; both replicated), `(!m.kind || m.kind === 'dm') && isToMe(m.to) && (!from || isFromPeer(m.from)) && !m.read` → read=true, readAt || now. Per message: `marked && isDmMessageRead(m)` → exp>0 && !expiresAt → `expiresAt = Math.min(now+exp, (m.ts||now)+Math.max(exp,3600000))` with NaN propagating like Math.min (serializes as null). `changed` → pruneDms + save (note: expiresAt stamps that don't drop a message are NOT saved — JS only saves on a length change, quirk preserved). `if (from) addCoins(resolveMemberRef(from) || from, 2.0)` — the READ pays the SENDER.
- **POST /api/dm/groups**: name slice60 AFTER trim || 'Group chat'; members = dedupe([myEmail, ...rawMembers.map(resolveMemberRef || String(m).toLowerCase().trim()).filter(Boolean)]) (Set insertion order, first occurrence wins); < 2 → 400 `need at least one other member`; id = `String(Date.now()) + '-' + Math.random().toString(36).slice(2,8)` (local `random_base36_6`, the zones.rs contract); `{ok:true, group}`.
- **POST /api/dm/group/leave** (singular "group"): groupId empty → 400 `missing groupId`; STRICT `g.id === groupId` findIndex → 404 `group not found`; members filtered by norm ≠ mine; empty → the group is spliced out. A non-array members field (JS TypeError → 500) degrades to leaving the group untouched.
- **POST /api/dm/clear**: `if (!cleared[norm]) cleared[norm] = {}` (a truthy non-object entry makes the JS property writes silent no-ops — mirrored by the as_object_mut guards, the save still happens, still 200); `all` → my groups (members.some norm) get 'group:'+id stamps + DM peers collected from kind-falsy-or-'dm' messages in BOTH directions (Set dedupe, falsy refs skipped); else `groupId` → 'group:'+String(body.groupId); else `with` → BOTH 'dm:'+normalizeEmail(withRaw) AND 'dm:'+normalizeEmail(withResolved); else 400 `missing target`.
- **POST /api/dm/expiry**: `Number(body.expiry) || 0` — NaN/undefined/'' all land on 0 (a garbage expiry CLEARS the window rather than 400ing); requested !== 0 && !options.includes → 400 `invalid expiry`; groupId branch: STRICT find → 404 `group not found` → memberNorms includes(norm) → 403 `not a member`; with branch: resolveMemberRef('')-as-exists-check → `!peerResolved || peerResolved === norm` → 400 `invalid peer`; `setChatExpiry(key, requested)` (valid windows store, everything else DELETES the key). requested > 0 → the in-place filter: group msgs match by STRING coercion (String(m.groupId) === String(wsGroupId) — deliberately looser than the strict find above; replicated), DM msgs via isMeM/isPeerM (no falsy gate — normalizeEmail(undefined)==='' never matches the non-empty norm), read msgs drop when hour-old / older-than-requested / readAt-age ≥ requested, else get expiresAt stamped; `pruned` true only when the LENGTH changed (JS quirk preserved: expiresAt stamps on kept messages are only saved when something dropped). The live `chat_expiry` WS broadcast is deferred to the /ws batch (documented divergence — the HTTP response is unaffected).
- **POST /api/dm/report**: verifyRecaptcha(body.recaptcha_token||'', ip, sid) → 400 `reCAPTCHA failed. Please try again.` (the handler-scope ip + the auth sid); chat_reports.json push `{id, reason slice500, reportedBy, ts, context: Array.isArray ? context.map({from, to, text slice2000, ts: Number||0, reported: !!m.reported}) : []}`; > 5000 → splice head.
- Method-check parity recorded in the module docs: /api/dm/send, /api/dm/inbox, mark-read, leave, clear, expiry, report have NO method gate in the JS (any verb runs the ladder — a GET parses the empty body to {} and proceeds); /api/dm/groups splits GET (list) from POST (create) with PUT/DELETE falling through to 404. Rust mirrors all of it.
- Dev-baseline limitation (per the batch methodology): all six endpoints return the global 403 `password required` (anon AND valid-sid) or 403 `csrf_blocked` (no-Origin mutations) on BOTH engines; the deep ladders are unit-verified by construction and stay behind the Step 14b Origin'd-POST gate (handler.rs:281 405 stub; Origin'd POSTs would WRITE on bun, so the probe never sends them).
- Gates: fmt clean, clippy `-D warnings` clean (after E0502 readAt-borrow hoists, the Option-truthy fix in report, and rand 0.9's `gen` → `random` deprecation); `cargo test --workspace` 67+11+20 green; `tests/parity_static.js` 102/102; 79-case probe byte-identical bun:6802 ↔ rust:6803 (55 + 24 new: groups anon/auth GETs + POST/PUT, inbox anon/auth GETs + with/group query variants + POST, mark-read GET/POST/bad-json, leave/clear/expiry/report POSTs + bad-json + 300KB oversized); `tests/endpoint_test.js` identical 118 pass / 71 fail on both (suite grew 175→189 with 14 new batch-2 cases, all passing; failing-set names diffed and identical).

## Step 11 batch 3 verification log (2026-09-14)

- The four attachment endpoints ported as `routes/dm/attachment.rs` (~1050 lines incl. 10 unit tests), wired into the dm dispatcher with JS method checks (upload/delete POST, serve/list GET; other verbs fall through to the unclaimed-path flow — which now correctly reaches the final 405 after the handler.rs fix below). Storage layout: `e2e_attachments.json` is a DB-routed doc (NOT in PRESERVED_DATA_FILES), blob files raw at `data/e2e_attachments/<id><ext>`; index entries `{id, file, name, size, mime, user, ts, expiresAt}` with TTL 2 days.
- The auth ladder DIFFERS from dm_auth and both are replicated: attachments authenticate via `authedEmailForRequest` = `_authSession?.email || emailFromSid(sid) || names[sid] || ''` lowercased (session-cookie email first, validated via auth::email_from_sid's generations check), while dm/send is names[sid]-only. Rate gates: upload [20,60], delete [30,60] with the shared id_key ladder.
- Three upload content branches (JS `req.formData()` for multipart, JSON base64, raw stream otherwise): multipart via the `multer` crate (the same parser axum's multipart feature is built on; futures-util std feature) — File-vs-string distinction by `file_name()` presence (Some = file part, None = 400 'no file provided'; empty field name → 'attachment', empty type → 'application/octet-stream', ≤0 → 400 'empty file'); JSON upload — JS-lenient base64 (`Buffer.from(s,'base64')` ignores non-alphabet bytes, accepts -/_ as 62/63, STOPS at the first '='), `data.includes(',') ? split(',')[1] : data` quirk (everything before the first comma discarded), name slice180 / mime slice80; raw stream — x-filename header || ?name || 'attachment' with STRICT decodeURIComponent (invalid %XX → 500 `Failed to upload attachment` / `URI error`), content-type slice80, NO empty-file check.
- Three distinct 413 quota shapes replicated: multipart pre-check (`Attachment exceeds 250MB maximum file size limit.` with max, before usage), multipart/JSON usage check (`Storage quota exceeded (250MB max total). You have used {toFixed(1)}MB. Delete old attachments to free up space.`), raw-stream usage check (same sentence WITHOUT the used-MB figure) — plus the raw declared-Content-Length pre-check (declared != 0 && total + declared > max, garbage header = NaN never trips). `js_to_fixed_1` replicates toFixed(1)'s ties-UP rounding ((1.25)→"1.3", (12.25)→"12.3", (0.05)→"0.1", NaN→"NaN") where Rust's {:.1} ties-to-even.
- Byte-exact helpers (bun-golden unit tests): `js_extname` (POSIX: '.hidden'→'', 'file.'→'.', '..foo'→'.foo'), `sanitize_ext` (extname().slice(0,10) BEFORE the [^a-zA-Z0-9._-] strip, empty → '.bin'; 'f.abcdefgh$ij' → '.abcdefgh'), `js_base64_decode` (five bun vectors incl. '===aGVsbG8=' → [], 'ab=cd==' → [105], 'a-b_c' → [107,230,255]), `decode_uri_component_strict`, `js_to_fixed_1` (incl. −1.25→"-1.2"), `multipart_boundary` (quoted-string strip).
- TTL sweeper (`clean_expired_in`): falsy entries DROPPED (not just expired ones), unparseable expiresAt (NaN) NEVER expires, blob unlinked, index saved fire-and-forget (`let _ = save_index`). Usage/sort semantics: total += truthy(size) ? Number.unwrap_or(NaN) : 0 (NaN poisons total, never trips quota); items keep present-null keys but drop absent ones (JSON.stringify undefined-drop); ts-desc sort with a NaN comparator → Ordering::Equal → STABLE original order (verified in bun).
- **Fixed a latent batch-2 divergence found in scope**: dm/inbox.rs's private `qs_get` didn't convert '+' to space (bun's URLSearchParams does); hoisted a corrected `qs_get`/`form_urlencoded_decode` to dm/mod.rs (lenient %XX passthrough per the bun probe) and removed the stale copy — query values containing '+' now match on both engines.
- **Fixed a latent batch-1 gap in main.rs**: the global 256KB body cap truncated /api/dm/send's ~2.3MB chat envelopes (and any >256KB body silently parsed to {}); replaced with path-aware caps (attachment/upload → 250MB + 8MB, dm/send → max_chat_json_body_bytes, else 256KB) and a `to_bytes` overflow → cap+1 zero bytes so route length checks fire like the JS readRequestTextLimited throw. **Documented deviation**: multipart uploads >264MB hit the memory-safety read cap → 500 in Rust vs JS's 413 (JS streams req.formData()).
- **Fixed a Step-4 handler.rs divergence the batch-3 probe caught**: the early `if !is_get_like { csrf; return 405 }` shortcut ran BEFORE the gates, but in JS the whole page/static section lives inside `if (method === 'GET')` (server.js:18619-24849) and unclaimed non-GET requests fall through CSRF + the password gate to the final errResp(405) at 24851. Removed the early return: non-GET requests now flow through all method-agnostic gates (CSRF → rate → IP ban → admin → maintenance → account ban → password gate) and land on the 405 only after the route dispatch declines — which is why the CSRF-exempt /api/dm/attachment/upload POST surfaces `403 password required` in dev instead of 405, matching bun. Strict `method != GET` at the fallthrough (HEAD/OPTIONS 405 too, like JS's GET-only branch).
- `POST /api/dm/attachments/delete`: ids array → truthy + trim-nonempty filter; else truthy(body.id) → [String(id).trim()] (NOT filtered); else [] → 400 `no attachments specified`; privileged = is_admin_email || is_moderator_email bypasses ownership; per-id unlink + remove; index save failure swallowed (documented); response `{success, deleted, total, max, items}`.
- `GET /api/dm/attachment` serve: id charset `^[a-zA-Z0-9_-]+$` → 400; 404 ladder (index miss / expired / file missing / unreadable all → same fallback strings); `Content-Disposition` inline|attachment by ?download with safeName quote/CR/LF-escaped + RFC 5987 filename* via encode_uri_component; cache-control private/no-store + nosniff.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 67+11+30 green (10 new attachment unit tests); `tests/parity_static.js` 102/102; 102-case probe byte-identical bun:6802 ↔ rust:6803 (79 prior + 23 new attachment cases: anon/auth GETs with id/download/traversal/junk-query variants, list anon/auth, upload no-Origin POSTs for the JSON/bad-json/multipart/raw branches, delete POSTs incl. ids-array/id-single/bad-json/oversized, wrong-verb fall-throughs); `tests/endpoint_test.js` identical 128 pass / 71 fail on both (suite grew 189→199 with 10 new batch-3 cases, all passing; failing-set names diffed and identical).

## Step 11 batch 4 verification log (2026-09-14)

- The eight E2E-secure-chat endpoints ported as `routes/e2e.rs` (~790 lines incl. 9 unit tests), wired into the handler 4c dispatch (after members, before dm — JS file order get-key 13739 < dm 19452) and into `workers.rs` with the 60s `e2eUsers` sweeper (entries older than 5 min dropped, server.js:4044-4049). State: `e2e_users` as an insertion-ordered `Vec<(String, E2eUser)>` (the JS object's `Object.entries` order; join overwrites in place) and `e2e_messages` as a `HashMap<String, Vec<E2eMessage>>` keyed by the sorted-pair `e2eKey` with a 500 cap per conversation (slice(-500) semantics via split_off).
- `mitch_lib::crypto::generate_p256_keypair` — genServerKeypair parity: the 32-byte scalar drawn from `random_bytes` with a retry on the (probability-zero) invalid-scalar case, public half exported as the raw 65-byte `04…` point hex (`ToEncodedPoint(false)`); `p256 = { workspace = true }` in mitch-server (the `SecretKey` type lives in `E2eUser` for the Step 11 `/ws` relay, though the JS never reads `priv_key` back today).
- **Auth ladders (two distinct, both replicated)**: get-key/register-key/verify-password use `names[sid]` — 401 `{success:false, message:'Auth required'}` then 403 `Email not found` on the lowercase+trim miss; join/heartbeat/send/messages use `normalizeEmail(emailFromSid(sid) || '')` → 401 `Auth required`. users/messages speak `{error}`; the other six speak `{success, message}`.
- **GET /api/e2e/get-key**: 404 gate is plain JS falsiness on the stored entry (`!entry`, so a null entry 404s like an absent one); top-level response in the JS object-literal order (success, pubKeyHex/encryptedPrivateJwk/ivHex as RAW passthrough with the JSON undefined-drop, kdfSaltHex/kdfIterations always via `|| ''`/`|| 0`, keyHistory). History entries add updatedAt; `history.slice(0,5)` only when an array.
- **/api/e2e/join**: nick = `(body.nickname || '')` strip `[^a-zA-Z0-9_@._-]` THEN slice(0,50); BAD_NICS (builtin 'awgnagae' + data/bad_words.json, compared as `nick.toLowerCase().includes(s)` WITHOUT lowercasing s) checked BEFORE the validity 400; validity = nick truthy && canonicalNick === email && isHex(pubKey,130) && startsWith('04') (case-sensitive); mint → overwrite-or-push keeping the JS insertion position; response nickname = maskEmail(email) which is a pass-through now (server.js:1380-1384).
- **/api/e2e/heartbeat**: tryParseJson FAILURE → `200 {success:true}` (the JS early-returns success); an EMPTY body parses to `{}` → nick '' → 403 Identity mismatch; last_seen stamped only when `nick in e2eUsers && normalizeEmail(e2eUsers[nick].email) === email`; always 200 at the end (whole handler try/catch{}).
- **POST /api/e2e/register-key**: Missing-fields gate on RAW destructured values' JS truthiness (a JSON null/0/false 400s); regexes `/^04[0-9a-f]{128}$/i`, `/^[0-9a-f]{24}$/i`, `/^[0-9a-f]+$/i` with UTF-16 length even + ≤16384; `String(body.kdfSaltHex || '').toLowerCase()` with the lowercase-only `^[0-9a-f]{64}$` check; `parseInt(body.kdfIterations, 10) || 0` (decimal prefix parse, NaN → 0) with the `kdfIterations && (<100000 || >2000000)` → 400 window; `body.createOnly === true` (strictly boolean) + previous.encryptedPrivateJwk truthy → 409 KEY_ALREADY_EXISTS; history = previous.history.slice(0,4) then unshift of the previous doc (RAW pubKeyHex compare `!==`) when previous.pubKeyHex truthy && different && previous.encryptedPrivateJwk && previous.ivHex truthy, then slice(0,5); stored doc keys in the JS literal order (pubKeyHex, encryptedPrivateJwk, ivHex, kdfSaltHex, kdfIterations, updatedAt, history) with the RAW body values (String() forms used only for validation); kdfIterations serialized integral (no trailing .0). E2E_KEYS_FILE = data/e2e_keys.json (DB-routed doc).
- **POST /api/e2e/verify-password**: `checkRateLimit(req, path)` runs BEFORE the cookie/auth gates (like /api/pass); `body.password || ''` → 400 `Password required` (truthy on the raw value); `loadPasswords()[normalizeEmail(email)]` — `!stored || !Bun.password.verify` → 401 `Incorrect password`; 200 `{success:true}` (argon2 PHC params embedded in the stored string, so the argon2 crate verifies whatever bun hashed).
- **/api/e2e/send**: destructured RAW values with defaults ONLY for missing keys (`{from: frm = '', to = '', data: msgData = '', iv = ''}`) — a JSON null stays null and the JS-truthiness gate 400s it; fromNorm !== email → 403; `!(fromNorm in e2eUsers) || normalizeEmail(e2eUsers[fromNorm].email) !== email` → 401 `Not registered`; `!toNorm || String(msgData).length > 256000 || !isHex(iv, 24)` → 400 `Invalid message` (JS .length = UTF-16 code units, replicated); entry `{from, to, data: String(msgData), iv: String(iv), timestamp}` pushed under e2eKey then 500-capped.
- **GET /api/e2e/users** (inside the JS GET branch): filter `now − last_seen < 60000`, map `{nickname: displayEmail(n), pubKey: u.pub_key}`; **GET /api/e2e/messages**: me/with normalized, `parseInt(qs.get('since') || '0') || 0`, Missing params 400 BEFORE the identity 403 (email empty OR me !== email), filter `timestamp > since`, map `{from, to, data, iv, timestamp}`.
- Method-check parity in the module docs: join/heartbeat/send have NO method check (any verb runs the ladder); get-key is GET-only, register-key/verify-password POST-only (each an explicit check whose failure falls through to the unclaimed-path 405); users/messages only GET (wrong verbs skip the JS GET branch and fall through).
- Gates: fmt clean, clippy `-D warnings` clean (Box<Response> on the ladder Errs for result_large_err, poison-tolerant `.lock().unwrap_or_else(|e| e.into_inner())` per the canvas pattern); `cargo test --workspace` 67+11+39 green (9 new e2e unit tests: e2e_key goldens, js_num_value, parseInt ladder, kdf window, regex ladder, history coalesce, nickname filter, UTF-16 length, sweep retain) — note the dm `resolve_member_ref_ladder` flake persists under cross-crate parallelism only (passes isolated 5/5, predates this batch); `tests/parity_static.js` 102/102; 134-case probe byte-identical bun:6802 ↔ rust:6803 (102 prior + 32 new e2e cases: get-key anon/auth + wrong-verb POST/PUT, register-key GET/POST/bad-json, verify-password GET/POST, join GET/POST/bad-json/PUT, heartbeat GET/POST/auth, send GET/POST, users GET anon/auth + POST + HEAD, messages anon/auth/no-params/params/mismatch/since-junk + POST, e2e fall-throughs); `tests/endpoint_test.js` identical 150 pass / 68 fail on both (suite grew 199→221 with 22 new batch-4 cases, all passing after correcting the dev-baseline expectations — unknown paths and HEAD both land on the pre-static password gate, so 403 not 404; failing-set names diffed and identical).

## Step 11 remainder verification log (2026-09-14)

- `/ws` broadcast socket ported as `routes/../ws.rs` (~490 lines incl. unit test) with the full surface: upgrade gates (same-origin → 403 `websocket origin rejected`; `studentId||id` + `validId`/`isRevoked`/`checkPasswordCookie` sid ladder → 401 `authentication required`; `emailFromSid` → 401; no upgrade extractor → 400 `websocket upgrade failed`), open leg (wasPresent BEFORE registry add, presence stamp keeping `playing`, fire-and-forget `notifyFriendsOnline` when newly online, `presence_changed` online broadcast), message leg (`presence_ping` re-validates sid+email and stamps lastSeen else close 1008 "Session expired"; non-ping frames swallowed like the JS try/catch), close leg (single-socket removal; last socket stamps lastSeen + 3s re-check task that deletes presence and broadcasts offline), and the 10s presence sweeper in `workers.rs`. `isUserPresent` gained the live socket leg (`hasAuthenticatedBroadcastSocket`) with the 45s map fallback; `touchUserPresence` ports cvOnline dual-key writes + the wasOffline/playing-changed broadcast ladder. The password gate already exempts `/ws` (handler.rs), so the arm's own gates gate anon upgrades — matching the JS flow.
- Transport: axum `WebSocketUpgrade` extracted from request parts in `main.rs::get_any` (`FromRequestParts`, Err → None) and threaded through `handler::handle` into `ws::handle_upgrade` (`on_upgrade` → per-socket task). Fan-out = `tokio::sync::broadcast` channel with send-time recipient sets (`All` | normalized-email `HashSet`) resolved by the caller — the same observable behavior as the JS per-socket send loop; a socket subscribes to the channel exactly at the `allSockets.add` point so it receives its own open-leg `presence_changed`.
- All deferred Step-9/10/11 fan-outs wired: `new_dm` (dm/send.rs — group roster or sender+peer), `chat_expiry` (dm/manage.rs — undefined-drop of groupId/with keys), 5× `canvas_delta` (pixel/bulk/erase/admin-erase/zones-clear via `canvas::broadcast_canvas_delta`, `{type:'canvas_delta', ...delta}` key order, >300-pixel chunk invalidation), 5× pickle payloads (chat/react/bulletin new/react/delete), `admin_broadcast`/`admin_jumpscare` (economy.rs direct endpoint + moderation.rs action dispatch, undefined-drop of `message`), `refresh_notifications` (me/notifications.rs mark-read). `presence/heartbeat` (misc.rs) + the `/api/ping` playingGame presence leg (proxy.rs, exact page ladder incl. case-sensitive `/games/<slug>` regex) ported. `profile_updated` deferred — `/api/profile` POST is unported (no Rust call site); game-portal heartbeat → Step 12.
- **Two live bugs the probe caught and fixed**: (1) the socket subscribed to the fan-out channel AFTER its own open-leg `presence_changed` broadcast (JS delivers it — allSockets.add precedes the send loop); (2) `close_leg` removed ALL sockets for an email instead of only the closing socket (`allSockets.delete(ws)` is per-socket), which fired the 3s offline timer while the user's other socket was still connected.
- Dev auth discovery: `getCookies` DELETES raw `studentId`/`id` cookies outside NODE_ENV=test (display-only legacy cookies), so live authed WS testing required a minted `mitch_session` (auth_sessions.json) — probes at /tmp/probe_ws.js, /tmp/probe_ws_raw.js, /tmp/probe_ws_live.js.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 67+11+40 green (no new failures; dm `resolve_member_ref_ladder` parallel flake persists, passes isolated — predates batch); `tests/parity_static.js` 102/102; **live WS probes byte-identical bun:6802 ↔ rust:6803**: 9-case raw-upgrade matrix (anon/cross-origin/garbage-sid/unknown-user/plain-GET → identical status+body; authed `mitch_session` → 101 both) and 16-case live probe (upgrade matrix; scripted-socket session sequence: open-leg self `presence_changed`, junk+ping frames keep the socket open, second same-user socket re-broadcasts to both, closing one of two sockets does NOT fire the offline timer, reconnect after full close re-broadcasts online; heartbeat CSRF/authed/bad-json/other-cookie-name + friends list/pending with the session; admin broadcast/jumpscare/missing-msg fan-outs observed on a live socket; `dm/send` → `new_dm` fan-out; `canvas/pixel` → `canvas_delta` fan-out); `tests/endpoint_test.js` identical 156 pass / 73 fail on both (suite grew 221→229 with 8 new /ws + presence/heartbeat cases, all passing; failing-set names diffed and identical).

## Step 12 batch 1 verification log (2026-09-14)

- Game-portal rewards + game stats/categories ported as `routes/games.rs` (~490 lines incl. 8 unit tests), wired into the handler dispatch after misc, before members. `GET /api/game-categories` (server.js:19081, the getGameCategories() base→local→external merge, fresh reads), `GET /api/game-stats` (server.js:19107, globalGameStats from data/global_game_stats.json — the /api/ping port already updates the same file), `POST /api/game-portal/heartbeat` (server.js:16338-16385 with `settleGamePortalHeartbeat` from lib/game_portal_rewards.js:18-48 — min 5s / max 45s delta gate, per-game accrual, 2 coins per complete minute, 240/day cap, game-switch + inactive accrual resets, cap-aware remainder).
- **Dead-code discovery: `GET /api/game-portal/status` (server.js:16319-16336) is unreachable in bun** — the handler sits INSIDE the `if (method === 'POST')` block spanning 13991-17779, so the GET-only endpoint can never match and bun 404s the path even with a valid session (verified live + debug-instance marker bisection on 6805). The port mirrors bun by NOT serving it; the portal frontend (webserver/game-portal/portal.js:326) still calls it and handles the 404 gracefully. Endpoint-test case asserts the 403 password-gate parity (the anon gate fires before the path is even claimed).
- State: `game_portal_sessions: Mutex<HashMap<String, GamePortalSession>>` in AppState keyed by normalized email (server.js:493). Heartbeat: strict JSON → 400 `bad json`; sid ladder auth (studentId||id → validId+revoked → emailFromSid, 401 `{authenticated:false}`); day reset via `gamePortalDayKey` (UTC ISO slice(0,10), via new `mitch_lib::coins::js_iso_date_from`); settle → addCoins(multiplier) → user_stats increments (game_portal_reward_today/minutes/coins) → `touchUserPresence(email, active ? "Playing ${game}" : "Browsing games")`; response coins/dailyEarned serialized integral via jsval::num_value.
- **Cross-cutting JS-number formatting fix**: serde_json prints integral f64 as `123.0` where JS prints `123` — found live (`"coins":0.0`) and fixed with `jsval::num_value`/`json!(0)`: misc.rs me_coins + leaderboard coins fallback, admin/economy.rs `newBalance` ×3 + `amount` ×7 (both response echoes and log_admin_action payloads). Verified live: `/api/me/coins` integral on both; `POST /api/admin/gift-coins` returns `"amount":1,"newBalance":1631` bun / `1632` rust (both integral, no `.0`).
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 67+11+48 green (8 new games unit tests: first-beat, inactive/game-switch resets, too-fast carry / too-slow reset, per-minute payout with carryover, daily-cap edge ×2, title normalizer incl. JS-\s set, control strip, UTF-16 60-slice); `tests/parity_static.js` 102/102; 19-case live differential probe byte-identical bun:6802 ↔ rust:6803 (categories/stats shapes, full heartbeat sequence at 8s spacing — first-beat 0, payout at ~1 min, preDaily-relative dailyEarned, >45s reset, game-switch reset, title normalization, inactive beat, dead status 404 HTML, game_portal stats fields, me/coins integral — 19/19 both); `tests/endpoint_test.js` identical **162 pass / 73 fail on both** (suite grew 229→235 with 6 new batch-1 cases: password-gate 403s for categories/stats/status×2, heartbeat anon-CSRF, heartbeat missing-CSRF; failing-set names diffed and identical).
- Rate limits noted: `/api/game-portal/status` [30,60], `/api/game-portal/heartbeat` [10,60] (heartbeat timing-exempt as in bun); probe spacing 8s to stay under the 10/min RL.

## Step 12 batch 2 verification log (2026-09-15)

- The six idle/mini games ported as `routes/games/{idle,mini}.rs` (idle ~940 lines, mini ~760, incl. unit tests) with `games.rs` → `git mv games/mod.rs` (batch-1 content unchanged) and dispatch `idle::handle` → `mini::handle` after the portal endpoints. **Adrian Clicker 2.0** (server.js:22866-22962 + helpers 618-826): state/create + offline apply (15s threshold without a lastTs stamp, 43200s cap, premium efficiency 1.0/0.5, SAFE_CAP 1e290), buy (tech vs upgrade via `cost || baseCost` JS truthiness, 1.15^(count+i) ladder with i==0 → 400 serverPoints, insufficient → 400), sync (claim loop floor(10*1.04^coins), playtime coins floor(session/600000), premium ×2, >max*3 → logCheat + clamp, client/max fallback) — **Richard's Riches** (22964-23056 + 683-766): state/{offlineGain}, sync with the 10 businesses (milestone-halving speed floor 0.05, managers gate, `{id}_1` ×3, all_1/2/3 globals, claim loop floor(100*3^coins), playtime pending ×(premium?2:1)×100, >max*3 && >100 → logCheat + clamp).
- **Mini games** (mini.rs): kodys-keyboard/payout (wpm>250 || ms<2000 → 400 Impossible speed; 60s cooldown → 429; 20/day → 429; tier coins ×premium ×1.5-friend-floor with the cvOnline 120s window; `friendBonusActive` DROPPED from the JSON when coins===0 — it's scoped inside the JS `if`), pennys-piano-keys/payout (500-tile cap, 135ms/tile floor, time-travel `actualElapsed < ms*0.8`, 1000-tile/day cap with allowedTiles clamping, zero-coins branch stamps lastTs + counts piano_games), sebastians-piccolo/payout (12000-point cap, estimatedTiles=score/12 speed check, 0.005 coins/point with a 1000/day coin cap, addCoins reason "Sebastian's Piccolo Symphony Performance"), lillians-logic state (any verb) + validate (NO auth, `logicDictionary.has(word.toLowerCase())`) + next-wordle (random from the 42-word pool, stores currentWordle) + solve (wordle: currentWordle truthy-match with `delete s.currentWordle` OR the ±24h date-word via seed y*10000+(m+1)*100+d; mines: 10s cooldown 429, 50/day, 100-cell validator ladder with exact error strings, neighbor-count check with `got ${cell.count}` raw-value interpolation; else 400 Unknown game type; premium ×2 at the end; `firstToday: type==='wordle' && firstToday`).
- **ECMAScript number formatting (the batch's core cross-cutting fix)**: idle-session responses echo passthrough state where values reach 1e260+ — serde_json's `1.0`/`1e22` diverged from JS's `1`/`1e+22`. `mitch_lib::data::js_number_string` now implements the full ECMAScript Number::toString (shortest round-trip digits via `format!("{:e}")`, then the k≤n≤21 zero-fill / 0<n≤21 split / -6<n≤0 zero-pad / exponential `1.4e+21` placement branches — verified against live bun for `2**60 → "1152921504606847000"` (zero-filled SHORTEST digits, not the exact integer), `1e21 → "1e+21"`, `1e-7 → "1e-7"`, `1e-6 → "0.000001"`, `NaN/Infinity → "null"` in JSON); compact `js_stringify`/`js_stringify_pretty` built on it; `errors.rs::json_resp_str` carries pre-serialized bodies with the exact jsonResp headers. The upgrade/tech/business tables echo **byte-identical** between bun and rust (`"baseCost":2.6e+22`, `"cost":1e+85`, `"baseSpeed":0.6`).
- **Local-time dates**: new `mitch_lib::jstime` (chrono 0.4 `clock` feature) — `js_to_date_string` (`Www Mmm DD YYYY`, the daily-counter key used by typing/piano/piccolo/logic) and `js_date_seed` (the wordle day word); dev hosts run America/Los_Angeles, which std cannot read.
- **Two latent rate-limit bugs found and fixed in scope**: (1) handler.rs 3b hardcoded `id_key = "anon"` — every authed request shared the anon floor (max/5); now ports `getIdKey` (server.js:5915-5920): session-restoring cookies → `studentId||id` → `id:<sid>` when validId passes, else anon. (2) e2e.rs verify-password called `rate_limit_check` a second time after 3b — bun's explicit `checkRateLimit` is a NO-OP there (`req._rateLimitChecked` stamped by the global prelude, server.js:5987), so the Rust copy was double-counting slots against the [5,60] entry; removed with a documenting comment.
- Session maps in AppState (`clicker/typing/logic/richard/piano/piccolo_sessions` + `logic_dictionary`): clicker/typing/logic/richard seeded from their DB documents at boot, piano/piccolo deliberately start EMPTY (bun never loads them, server.js:25252-25255); write-through via `write_document` (js_stringify_pretty = bun's `JSON.stringify(data,null,2)`).
- Idle/mini sid ladder (`sid_email` + `games_email` in games/mod.rs): `studentId||id` → validId → emailFromSid with NO revoked-id check (unlike the game-portal pair) and per-endpoint 401 bodies — adrian/richard/lillians-state `{error:'auth required'}`/`{error:'identity missing'}`, kodys `{success:false}`, piano/piccolo/next-wordle/solve `{success:false,error:'Authentication required'}`/`'Invalid identity'`.
- **Table constants**: `ADRIAN_TECH`/`ADRIAN_UPGRADES`/`RICHARD_BUSINESSES` as `LazyLock<Vec<(&str, Value)>>` in JS object-literal order (serde_json 1.0.151 cannot const-eval `json!`). `getRichardPower` parity note: `new Set(s.upgrades || [])` over an OBJECT has the object itself as its single element → every has() false; the port only matches array members.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 70+11+58 green (10 new idle/mini unit tests: power/speed tables via js_stringify echoes, adrian power math, richard speed+power, parseInt semantics incl. 0x-hex, toExponential +, neighbor shape, malformed-cell bool typing, raw count message, date-string/seed shapes, js_number_string ECMAScript vectors); `tests/parity_static.js` 102/102; **44-case live differential probe byte-identical bun:6802 ↔ rust:6803** (/tmp/probe_idle_games.js: all 8 no-auth ladders, 3 validate cases, 6 anti-cheat gates, 6-step mines validation ladder, date-word solve, adrian state/buy-unknown/buy-broke/sync/cheat-clamp, richard state/sync/cheat-clamp, kodys payout+cooldown, pennys payout+zero-branch, piccolo payout+thresholds, mines solve+cooldown; static upgrade/tech tables byte-compared from raw responses; fresh-state echoes normalized only for lastTs/startTime); `tests/endpoint_test.js` identical **176 pass / 73 fail on both** (suite grew 235→249 with 14 new batch-2 cases: password-gate 403s for all 12 endpoints + kodys missing-CSRF + wrong-verb; failing-set names diffed and identical).
- Rate limits: NO RATE_LIMITS entries for any idle/mini path → `__default__ [100,60]` via getIdKey buckets; the explicit `checkRateLimit` calls inside next-wordle/solve are no-ops in bun (global prelude stamps `_rateLimitChecked` first).

## Step 12 batch 3a verification log (2026-09-15)

- Casino prelude + shared helpers + history/global-feed + four instant games (rock-paper-scissors, lucky-seven, color-card, triple-dice) ported as `routes/casino.rs` (~700 lines incl. unit tests), wired into the handler dispatch after the games group. Prelude ladder (server.js:23077-23175): `/api/casino/` prefix → sid ladder 401 `{error:'unauthorized'}` (validId) / 401 `{error:'email not found'}` (emailFromSid — NO revoked-id check, unlike the game-portal pair); `casinoReadOnly = ['/api/casino/history','/api/casino/global-feed','/api/casino/blackjack/state']` GET-only, everything else POST → 405 `{error:'Method not allowed'}`; POSTs (except blackjack/hit + blackjack/stand, unported here) run tryParseJson (400 `{error:'bad json'}`, empty body → `{}`) + verifyRecaptcha (400 `{error:'reCAPTCHA failed. Please try again.'}`); unknown `/api/casino/*` paths fall through to the global 404 exactly like bun's dangling block.
- Helpers: `readCasinoBet(min=1)` ladder (min → 'Minimum bet is ${min} coins.', bal → 'You do not have enough coins for that bet.', non-VIP >500 → 'Maximum bet is 500 coins. Buy a VIP Casino Pass in the shop for unlimited betting!') with the bet normalized via `Number(Number(amount).toFixed(2))`; `settleCasinoRound` (2X DOUBLE from stats double_down_until, REFUNDED (INSURED) on zero payout when insured, safePayout = max(0,·).toFixed(4), net = (safePayout − effectiveBet).toFixed(4), casinoIntake/Payout atomics + data/casino_stats.json write-through, addCoins, casinoHistory unshift 25-cap, bettingFeed unshift 50-cap, casino_bets/casino_wins stats); `addHistory`/`logBet` caps; history `{history:[...]}` and global-feed `{feed:[...]}` (user → email local-part, 'anonymous' fallback).
- **ECMAScript toFixed (cross-cutting fix)**: new `mitch_lib::data::js_to_fixed(x, digits)` — exact decimal rounding from the shortest-repr digits with 30 guard digits, ties round half-up on the digit string (ECMAScript picks the larger n for exact ties; `2.675 → "2.67"` because the stored double is 2.67499…, `-0.0 → "0.00"` unsigned, `9.99996 → "10.0000"` carries into the integer part); `js_num_from_fixed(s)` = `s.parse::<f64>()`. Verified 12 vectors against live bun. This is what keeps `bet = Number(Number(amount).toFixed(2))` and the settle rounding byte-equal: amount `2.005` bets **2** on both servers (`(2.005).toFixed(2) === "2.00"` — the stored double is 2.00499…), asserted by the probe.
- **Math.random port (cross-cutting fix)**: `mitch_lib::crypto::js_random()` (53-bit uniform [0,1) — `u64 >> 11 / 2^53`) + `js_random_index(len)`; mini.rs now uses the shared helper instead of its local copy.
- State: `bj_games` (BjGame, populated by the blackjack commit — `/api/casino/blackjack/state` already serves `{active:true,bet,playerHand,dealerUpCard}` / `{active:false}`), `casino_history: Mutex<HashMap<String, Vec<Value>>>`, `betting_feed: Mutex<Vec<Value>>` in AppState. `is_rigged()` NOT ported yet — first caller is roulette (clippy dead-code); BjGame carries `#[allow(dead_code)]` until then.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 71+11+61 green (3 new casino unit tests: ECMAScript toFixed vectors, RPS/card payout + settle math, triple-dice mult table; mitch-lib dm tests remain the known parallel-only flake and pass serially); `tests/parity_static.js` 102/102; **83-case live differential probe bun:6802 ↔ rust:6803** (/tmp/probe_casino.js: 30-step validation ladder byte-compared — 405s, bad json, empty body, missing/0/0.5/−1/NaN/null/huge amounts, bad choices ×2 games, pick 0/7/2.5/'x', unknown-game fall-through; 14 happy-path plays with per-side exact money math (win === round4(bet×mult), net === round4(win−bet), newBalance === round4(prevReadBeforePlay + net), bet rounding 2.005→2, string amounts, VIP 501 cap) + cross-side structural key-order/type compare; history length/game-sequence/entry-shape/key-order/outcomes; feed cap/local-part/shape; probe users reseeded to 1000 coins via DB row + dual restart — bun caches coins at boot). **Probe-tooling gotchas recorded**: the CSRF gate needs `X-Mitch-Requested-With: 1` AND `Origin: <base>` (the `fetch` value is not `1`), and bun's anti-bot timing detector 429s bursts with <50ms spread — the probe jitters 40–360ms; piccolo-payout top-ups require real elapsed time (ms>5000 ⇒ actualElapsed ≥ ms×0.8) so coins are seeded directly instead.
- `tests/endpoint_test.js` identical **188 pass / 70 fail on both** (suite grew 249→258 with 9 new batch-3a cases: history/global-feed/rock-paper-scissors password-gate 403s, history/global-feed/triple-dice/color-card csrf-gate 403s, history+global-feed+blackjack/state anonymous 403s; failing-set names byte-identical to the pre-batch baseline — md5 aa3efbcb0142ce21b31adac6cac5a036 — i.e. no regression, only additions). The legacy-id-cookie USER_TOKEN cases assert the dev gates (password gate on GET, csrf gate on no-Origin POST); handler bodies are verified live by the 83-case probe with minted sessions.
- Rate limits: NO RATE_LIMITS entries for any `/api/casino/*` path → `__default__ [100,60]` per ip + id bucket; the anti-bot timing detector applies (both sides verified live — 4 rapid casino POSTs 429 identically on bun and rust).

## Step 12 batch 3b verification log (2026-09-15)

- Plinko, roulette and high-low ported into `routes/casino.rs` (dispatch + handlers + 4 new unit tests; module doc now covers all 7 instant games). **Plinko** (server.js:23241-23272): `weightedPick` over the 7-slot table (labels `0x/0.5x/0.8x/1.2x/1.5x/3x/8x`, mults 0/0.5/0.8/1.2/1.5/3/8, weights 25/25/18/15/10/5/2, total 100) — the port zips label+mult+weight into `Vec<((&str, f64), f64)>` and shares `weighted_pick` (elided-lifetime generic); WIN iff mult ≥ 1; game name 'Plinko'; response `{ok, slot, mult, win, net, newBalance}`.
- **Roulette** (23274-23304): tryParseJson again (cached no-op via `parsedJsonBody` since the prelude already parsed), bet ladder, then `num = floor(random*37)`, `color = ROULETTE_COLORS[num]` computed BEFORE type validation (bun's order — validation errors cost the roll, observable via the 400 path's identical behavior); `type` is raw: strict `'red'`/`'black'` → mult 2, won iff color===type; `'green'` → mult 35, won iff num===0; else `Number(type)` integer 0-36 → mult 35, won iff num===type-number (accepts `true`→1 and `null`→0 as number bets, rejects `'5.5'` → 400 'Invalid bet type.'); `is_rigged()` ported (`const isRigged = () => false`, hardcoded) with its dead rig rewrites — red/black → first key with the OTHER color (num becomes a STRING key in the response), green → num 1/'red', number → (num+1)%37; response `{ok, number, color, won, mult, win, net, newBalance}`. `ROULETTE_COLORS[37]` unit-tested against the exact JS table (18 red / 18 black / 1 green).
- **High-low** (23306-23330): bet ladder, `choice = String(body.choice||'').toLowerCase()` → 400 'Choose higher or lower.' otherwise; `value = 1 + floor(random*13)`; push only on 7; won = higher ? value>7 : value<7; payout push ? bet : won ? bet*2 : 0; game name 'High / Low'; response `{ok, choice, card, value, push, won, win, net, newBalance}`. `card_name` (1→'A', 11/12/13→J/Q/K) extracted and color-card refactored onto it.
- **Probe tooling hardening (recorded for future batches)**: (1) `/api/me/coins` is RATE_LIMITED [30,60] (server.js:1538) — per-play prev reads blew the budget on back-to-back runs and every subsequent GET 429'd (`prev undefined`); the probe now tracks the balance chain from each play's authoritative `newBalance` and verifies the chain once against `/api/me/coins` at the END of each side ('balance chain bun/rust'). (2) **Achievement bonuses explain the recurring ±100/±250/±500 balance jumps** (previously logged as a mystery drift): `updateStat` fires `awardAchievement` on cumulative stats (casino_novice +100 @10 bets, casino_regular +500 @100 bets, casino_winner +250 @50 wins — server.js:3051-3074, 3405-3421) with the addCoins 'unspecified' reason; confirmed via logs/coins.log entries (`+250.0000 … unspecified` 0.7s after a plinko play). `/tmp/reset_probe_env.js` now zeroes casino_bets/casino_wins and PRE-AWARDS the three casino achievements for the playing users (already-earned achievements pay nothing) alongside the coin reseed, all via the json_documents DB rows (coins/user_stats/achievements are NOT in PRESERVED_DATA_FILES — editing data/*.json files directly does nothing). (3) Cross-side shape() now masks `slot` and `color` (random-derived strings) like computer/suit/card/dice.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 71+11+65 green (4 new casino unit tests: roulette color table, card_name 1-13, high-low outcome matrix incl. push-only-on-7, plinko slot table weights-sum-100 + 4 win slots; mitch-lib dm tests remain the known parallel-only flake, pass serially); `tests/parity_static.js` 102/102; **133-check live differential probe 0 fail** (/tmp/probe_casino.js: 42-step validation ladder byte-compared incl. plinko GET/1e9, roulette bad-json/no-amount/purple-type/missing-type/1e9, high-low GET/no-amount/BOTH/1e9; 24 plays with per-side exact money math — plinko generic bet×mult payout, roulette won?bet×mult, high-low push/won branches — game-logic verification per side (plinko table, roulette color-table + per-type mult/won incl. Number(type) for numeric bets, high-low card/value/push/won matrix), cross-side structural key-order/type compare, tracked balance chain verified against the server, history length 25 (24 plays + the batch-3a vip-cap exact) game-sequence/entry-shape/key-order, feed cap/local-part/shape; probe environment reset via /tmp/reset_probe_env.js + dual restart).
- `tests/endpoint_test.js` identical **196 pass / 70 fail on both** (suite grew 258→266 with 8 new batch-3b cases: plinko/roulette/high-low password-gate 403s, roulette/high-low csrf-gate 403s, plinko/roulette/high-low anonymous 403s; failing-set md5 aa3efbcb0142ce21b31adac6cac5a036 unchanged — no regression, only additions). Handler bodies verified live by the 133-check probe with minted sessions.
- Rate limits: unchanged from batch 3a — no RATE_LIMITS entries for `/api/casino/*` → `__default__ [100,60]`; anti-bot timing detector applies to all of them.

## Step 12 blackjack batch verification log (2026-09-15)

- Blackjack start/hit/stand ported into `routes/casino.rs` (three handlers + dispatch + `BjGame` state + 4 new unit tests; module doc now covers blackjack too). **Start** (server.js:23323-23359): its OWN bet ladder — `Number(body.amount)`, `!isFinite || <1 || > bal` → 400 `invalid bet` (NOT readCasinoBet: no toFixed normalization, no min message, raw `> bal` check), non-VIP >500 → 400 `Maximum bet is 500 coins. Buy a VIP Casino Pass in the shop for unlimited betting!`; order is casinoEnabled → 403 `Casino is currently closed.` → tryParseJson re-call (cached no-op) → `bjGames.has(norm)` 409 `Finish your current blackjack hand first.` (BEFORE bet validation) → ladder; casinoIntake += bet directly, addCoins(−bet) (no settle), 52-card deck (`{s:'♠|♥|♦|♣', v:'A|2..10|J|Q|K'}` suit-major) Fisher-Yates from the end (`i 51→1, j = floor(random*(i+1))`), playerHand 2 pops + dealerHand 2 pops, local `const rigged = false` with the dead rig branches ported under `if g.rigged`; response `{ok, playerHand, dealerUpCard: dealerHand[0]}`. **Hit** (23360-23406): skips parse+recaptcha; no game → 400 `no active game`; card = deck.pop; bust (hand_value > 21) → delete game + settle('Blackjack', bet, 0, 'BUST', false, true) + response `{ok, gameOver:true, playerHand, status:'bust', dealerHand, winAmt: 0}` — winAmt is the LITERAL 0, not settled.payout (an insurance refund would be invisible here); else `{ok, gameOver:false, playerHand, status:'active'}`. **Stand** (23408-23456): no game → 200 `{ok:true, gameOver:true}` (no status/winAmt fields); dealer draws while dval < 17; outcome `dval>21 || pval>dval → winAmt=bet*2 'win'; dval===pval → winAmt=bet 'push'; else 0 'lose'`; settle(..., res.toUpperCase(), false, true); response `{ok, gameOver:true, playerHand, dealerHand, status:res, winAmt: settled.payout}`. `hand_value` ports getVal (23393-23398): aces flex ((v+11≤21)?11:1 sequentially), faces 10, else parseInt(c.v) (unparseable v poisons NaN).
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 71+11+69 green (4 new casino unit tests: hand-value vectors incl. A,A=12 / A,K=21 / A,A,A=13 / 5,A,A=17 / Q,A,4=15 / A,2,3,4=20 / 10,10,A,A=22 / non-numeric-v → NaN; deck = 52 unique cards suit-major ♠A-first ♣K-last; stand outcome matrix win×2/push/lose; dealer draw-to-17 stop; mitch-lib dm tests remain the known parallel-only flake, pass serially); `tests/parity_static.js` 102/102; **167-check live differential probe 0 fail** (/tmp/probe_casino.js now with blackjack: 43-step ladder + bj start GET 405; 39 plays — bj start 10 → 409 duplicate → hit → stand → hit/stand no-game (400 'no active game' / 200 {ok,gameOver:true} with NO status field) → start 5 → hit ×2 → stand → start 0/'abc' 400 `invalid bet` → start 501 VIP-max string → start 10b → hit ×3 → stand; per-side state-dependent verification: start deducts bet and returns 2-card hand + upcard, hit grows hand to ≤21 or busts with winAmt literally 0 + dealerHand 2 cards, stand dealer dval≥17-or-bust with the win/push/lose winAmt matrix against bjVal (the probe's own getVal port), balance tracked from bet deductions + winAmt additions and verified against /api/me/coins once per side; cross-side shape compare masks playerHand/dealerHand/dealerUpCard as 'hands' and status as random; history run-window checks compare the non-blackjack game sequence cross-side and count per-side Blackjack entries against that side's settle count with outcome↔amount sign validation WIN>0 / PUSH=0 / LOSE,BUST<0; feed unchanged). **Probe-design constraints recorded**: (1) blackjack hit/stand are state-dependent across sides (a random mid-hand bust turns later plays into no-game responses) — cross-side shape compare is skipped for hit/stand, per-side verification only; (2) each start hand settles exactly once, so non-bj plays + 3 starts must stay under the history 25-entry cap (22 + 3 = 25); (3) /tmp wipes persist mid-session — mint_session.js / reset_probe_env.js / probe_casino.js all recreated from transcript twice this batch.
- `tests/endpoint_test.js` identical **200 pass / 73 fail on both** (suite grew 266→270 with 4 new blackjack-batch cases: blackjack/start csrf-gate 403, blackjack start/hit/stand anonymous 403s; the legacy USER_TOKEN blackjack flow cases predate the port and fail identically on both sides — part of the stable baseline; failing-set md5 aa3efbcb0142ce21b31adac6cac5a036 unchanged — no regression, only additions). Handler bodies verified live by the 167-check probe with minted sessions.
- Rate limits: unchanged from batch 3a/3b — no RATE_LIMITS entries for `/api/casino/*` → `__default__ [100,60]`; anti-bot timing detector applies (probe jitters 40–360ms); `/api/me/coins` [30,60] stays out of the per-play path.

## Step 12 poker batch verification log (2026-09-15)

- `POST /api/casino/poker/start` ported into `routes/casino.rs` (`poker` + `poker_rank` + dispatch + 1 new unit test). **Poker** (server.js:23460-23518): casinoEnabled → closed; tryParseJson; readCasinoBet (the normalized ladder, unlike blackjack's); 52-card deck built vals-ascending `2..A` (unlike blackjack's A-first `build_deck`), Fisher-Yates from the end, hand = 5 pops; `checkHand` ported as `poker_rank` — vMap 2-14, group sizes from the counts object sorted descending, isFlush = any suit count 5, isStraight = every consecutive +1 (**no ace-low straight** — A is always 14, so A-2-3-4-5 loses); ladder Royal Flush 500 → Straight Flush 100 → Four of a Kind 50 → Full House 15 → Flush 10 → Straight 7 → Three of a Kind 5 → Two Pair 3 → Jacks or Better 2 (single pair pays only J/Q/K/A — `vMap[v] >= 11`) → Lose 0; the dead rig re-draws WITH replacement from the remaining deck (≤20 attempts, ported under `is_rigged()`); settle `('Poker (' + rank + ')', bet, bet*mult, WIN/LOSE)` non-prepaid; response `{ok, hand, rank, mult, win, net, newBalance}`.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 71+11+70 green (new `poker_rank_ladder` test: all 10 ladder branches incl. the ace-low NON-straight and the low-pair-pays-nothing rule; this run even passed mitch-lib in parallel — the dm flakes remain intermittent); `tests/parity_static.js` 102/102; **171-check live differential probe 0 fail** (/tmp/probe_casino.js: ladder +4 poker steps (GET 405, bad json, amount 0 → `Minimum bet is 1 coins.`, 1e9 → VIP cap) byte-compared; plays −4 redundant (rps scissors, triple-dice 6, roulette black, roulette num 5 — keeps the history 25-cap headroom at 21 non-bj + 3 bj settles = 24) +4 poker plays (amount 1, 2.005→bet 2, '3'→bet 3, 5) with per-side rank-table verification (5-card hand of legal suit/value pairs, rank ∈ ladder, mult === rankMult[rank]) + the generic bet×mult money math; shape() masks `hand` as 'hands' and `rank` as random; the history sequence check normalizes `Poker (…)` names before the cross-side compare since the rank is random).
- `tests/endpoint_test.js` identical **202 pass / 73 fail on both** (suite grew 270→272 with 2 new poker cases: poker/start csrf-gate 403 + anonymous 403; failing-set md5 aa3efbcb0142ce21b31adac6cac5a036 unchanged — no regression, only additions). Handler bodies verified live by the 171-check probe with minted sessions.
- Rate limits: unchanged — no RATE_LIMITS entry for poker/start → `__default__ [100,60]`; anti-bot timing detector applies.

## Step 12 coinflip/dice/crash batch verification log (2026-09-15)

- Three called-side games ported into `routes/casino.rs` (handlers + dispatch; no new unit tests — the handlers are thin over the already-tested `read_casino_bet`/`settle_casino_round`/`js_to_fixed` helpers). **Coinflip** (server.js:23519-23534): `String(body.side||'').toLowerCase()` → 400 `Choose heads or tails.`; `result = random < 0.5 ? 'heads' : 'tails'`; won === side; mult 1.9; game 'Coin Flip'. **Dice** (23535-23553): side under/over → 400 `Choose under or over.`; `roll = floor(random*100)+1`; won = under ? roll<50 : roll>51 (50 and 51 always lose both ways); mult 1.94; game 'Dice Duel'; rig re-rolls a winning under into [50,100] and a winning over into [1,51] (dead, ported). **Crash** (23554-23569): readCasinoBet BEFORE the target check; `target = Number(body.target)` → `!isFinite || <1.2 || >6` → 400 `Cashout must be between 1.20x and 6.00x.` (bounds checked before the cashout's own toFixed(2)); `crashAt = Number(max(1, min(10, 0.95/max(random, 0.000001))).toFixed(2))`; `cashout = Number(target.toFixed(2))`; won = cashout <= crashAt (ties win); payout = bet×cashout, outcome WIN/CRASH; rig lowers crashAt to max(1, cashout−0.01) when the player would win (dead, ported); response `{ok, crashAt, target: cashout, won, mult: won?cashout:0, win, net, newBalance}` — mult is the cashout itself, so the generic bet×mult money math holds.
- **Probe tooling moved out of /tmp** (wiped for the third time mid-batch): `~/probes/{probe_casino,mint_session,reset_probe_env}.js` + `~/probes/token_probe_idle_{b,b2,r,r2}.txt` are now the canonical copies; probe_casino reads tokens from there. Also recorded: `kill $(pgrep -f 'bun server.js')` matches the invoking shell's own command line and kills it (exit 144) BEFORE later statements run — a stale mitch-server then holds 6803 and the restarted binary panics `Address already in use`, so live probing hits the OLD binary; kill by port (`kill $(lsof -ti :6802) $(lsof -ti :6803)`) instead.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 71+11+70 green (mitch-lib dm flake fired once in parallel, passes serially); `tests/parity_static.js` 102/102; **177-check live differential probe 0 fail** (/tmp→~/probes/probe_casino.js: ladder +6 deterministic steps (coinflip GET 405 + bad side → `Choose heads or tails.`, dice bad side → `Choose under or over.`, crash target 1.1/7/'abc' → `Cashout must be between 1.20x and 6.00x.`); plays +7 (coinflip heads/tails, dice under/over, crash 1.5/2.5/string '2.5' — the string target covers `Number(body.target)` coercion) −7 redundant (rps '3', color-card black/2.005, triple-dice str pick, plinko 5, roulette type bool, high-low lower) keeping 21 non-bj + 3 bj settles = 24 under the history 25-cap; per-side logic verification (result/won/mult for coinflip, roll 1-100 with the 50/51 always-lose boundary for dice, crashAt 1-10 with target === Number(target.toFixed(2)) and won = cashout≤crashAt for crash); shape() masks `result`/`roll`/`crashAt` as random).
- `tests/endpoint_test.js` identical **208 pass / 73 fail on both** (suite grew 272→278 with 6 new cases: coinflip/dice/crash csrf-gate 403s + anonymous 403s; failing-set md5 aa3efbcb0142ce21b31adac6cac5a036 unchanged — no regression, only additions). Handler bodies verified live by the 177-check probe with minted sessions.
- Rate limits: unchanged — no RATE_LIMITS entries → `__default__ [100,60]`; anti-bot timing detector applies.

## Step 12 wheel/scratch batch verification log (2026-09-15)

- Two weighted-outcome games ported into `routes/casino.rs` (handlers + dispatch + 2 new unit tests). **Prize Wheel** (server.js:23570-23590): `WHEEL_SEGMENTS` const of 7 label/mult/weight tuples in JS literal order (Bust 0×85, Half Back 0.5×40, Small Win 1.25×32, Double 2×31, Triple 3×8, Meteor 8×3, Galaxy Jackpot 20×1 — weights sum 200), zipped into `Vec<((&str, f64), f64)>` pairs and run through the shared `weighted_pick`; the dead rig branch forces segment[0] (Bust) whenever the picked mult ≥ 1, ported under `is_rigged()`; settle `('Prize Wheel', bet, bet*mult, mult≥1?'WIN':'LOSE')`; response `{ok, segment, mult, win, net, newBalance}` — no `won` field. **Scratch Card** (23591-23614): `roll = random()`, rig shift `roll < 0.180 → 0.300 + random()*0.7` (dead); band ladder extracted as `scratch_rank(roll)` — strict `<` thresholds (<0.002 → (90,'Triple Diamonds'), <0.010 → (25,'Triple Sevens'), <0.040 → (6,'Triple Bells'), <0.100 → (2.5,'Triple Fruit'), <0.180 → (1.5,'Small Match'), <0.300 → (1.0,'Half Back'), else (0,'No Match')) — exactly-at-threshold falls into the NEXT band; 9 tiles drawn from the 7-symbol table ('🍒','🍋','🍊','🍇','🔔','💎','7️⃣') with winning-band stamping: mult≥25 → tiles[0]=[4]=[8] = mult≥90?'💎':'7️⃣', elif mult≥2.5 → tiles[1]=[4]=[7] = mult≥6?'🔔':'🍒', elif mult>0 → tiles[3]=[4]='🍋'; settle `('Scratch Card', bet, bet*mult, mult≥1?'WIN':'LOSE')`; response `{ok, tiles, rank, mult, win, net, newBalance}` — also no `won` field.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 71+11+72 green (2 new casino unit tests: `wheel_segment_table_matches_js` — table literal-order equality, weights-sum-200, five WIN segments (first draft asserted 3 — Small Win/Double/Triple/Meteor/Galaxy all pay ≥1, i.e. five); `scratch_rank_thresholds` — exact boundary rolls incl. 0.002/0.010/0.040/0.100/0.180/0.300 and the sub-threshold probes 0.1799→Small Match / 0.0099→Triple SEVENS (first draft wrongly expected bells — 0.0099 is ≥0.002 and <0.010); both failures were test bugs, handler code was correct); `tests/parity_static.js` 102/102; **179-check live differential probe 0 fail** (~/probes/probe_casino.js: ladder +2 deterministic steps (wheel GET 405, scratch GET 405 — neither game has validation beyond readCasinoBet, so no error-ladder steps); plays +4 (wheel 1/2.005→bet 2, scratch 1/2.005→bet 2) −4 redundant (roulette green, roulette num str, poker 2.005, poker 5) keeping 19 non-bj + 3 bj settles = 22 under the history 25-cap; per-side logic verification — wheel: segment ∈ table labels && mult === table mult (shape() masks `segment` as random); scratch: rank ∈ the 7 band strings && mult === band mult && tiles = 9 symbols from the 7-symbol table && the winning-band stamping (diagonal triple 💎/7️⃣ ≥25, 🔔/🍒 ≥2.5, 🍋 pair >0); **probe-structure lesson recorded**: the wheel/scratch logic checks must run AFTER the generic money-math block, not before — first run failed 6 checks because (1) the logic blocks referenced a `won` field that these responses don't carry, and (2) an early ok=false skipped the money-math block's `bal = j.newBalance` chain update, cascading a bogus balance mismatch into the NEXT play).
- `tests/endpoint_test.js` identical **212 pass / 73 fail on both** (suite grew 278→282 with 4 new wheel/scratch cases: csrf-gate 403s + anonymous 403s; failing-set md5 aa3efbcb0142ce21b31adac6cac5a036 unchanged — no regression, only additions). Handler bodies verified live by the 179-check probe with minted sessions.
- Rate limits: unchanged — no RATE_LIMITS entries → `__default__ [100,60]`; anti-bot timing detector applies.

## Step 12 keno batch verification log (2026-09-15)

- `POST /api/casino/keno` ported into `routes/casino.rs` (handler + dispatch + 3 new unit tests). **Keno** (server.js:23615-23653): casinoEnabled → closed; tryParseJson; readCasinoBet BEFORE the picks check; `drawUniqueNumbers(max, count)` (server.js:23168-23175) ported as `draw_unique_numbers` — 1..max, Fisher-Yates from the end, first `count`, sorted ascending (first caller of the shared helper, now shared like `weightedPick`); picks parser extracted as `parse_keno_picks` — non-array → `[]`, else `Number()`-coerced (jsval::number, NaN for non-numeric strings), Set-deduped (insertion order, SameValueZero incl. NaN), filtered to `Number.isInteger && 1-40` (NaN/±Inf/floats/0/41 drop), sorted ascending; `<3 || >10` → 400 `Pick 3 to 10 numbers.`; `KENO_PAYOUT` const table (8 rows, picks 3-10 → hit-count → mult, JS literal order, absent cells 0) as `keno_payout(picks, hits)`; 12 balls; hits are picks-order (both ascending); the dead rig re-draws while the payout cell exists (≤20 attempts); paying mult × 1.12 through `js_to_fixed(m*1.12, 2)` (2.1→2.35, 0.9→1.01, 2800→3136); settle `('Keno Rush', bet, bet*mult, mult>0?'WIN':'LOSE')`; response `{ok, picks, drawn, hits, mult, win, net, newBalance}`.
- Gates: fmt clean, clippy `-D warnings` clean; `cargo test --workspace` 71+11+75 green (3 new casino unit tests: `keno_payout_table_matches_js` — full literal-order table + row invariants (hit counts ascend, first paying row ≥ 2 — a single hit never pays) + absent-cell zeros + the ×1.12 toFixed(2) boost vectors; `draw_unique_numbers_shape` — 50 draws: len 12, ascending, unique, integers 1-40 + the count>max degenerate case; `keno_picks_parser` — dedupe/sort, Number coercion ('5'/true→1/null→0 filtered, 'abc'→NaN dropped), float/0/41 filter, non-array → []; the mitch-lib dm flake fired once in parallel, passes serially); `tests/parity_static.js` 102/102; **187-check live differential probe 0 fail** (~/probes/probe_casino.js: ladder +8 deterministic steps (keno GET 405, bad json, no picks / picks 2 / picks 11 / picks non-array / dedupe-to-2 / filtered-to-2 — all → 400 `Pick 3 to 10 numbers.` byte-compared); plays +2 (keno 1-8, keno 2.005→bet 2 with picks 1-10) −2 redundant (wheel 2.005, scratch 2.005) keeping 21 non-bj + 3 bj settles = 24 under the history 25-cap; per-side logic verification — picks === sorted-unique-filtered parse of the body, drawn = 12 unique ascending integers 1-40, hits === picks∩drawn in picks order, mult === Number((table[picks][hits]×1.12).toFixed(2)) or 0; **probe-structure lessons recorded**: (1) shape()'s Array.isArray branch fires BEFORE the rand list, so `hits` (random length — 2 vs 1 bun/rust in the first run) must be special-cased inside the array branch, `picks`/`drawn` are deterministic lengths and stay 'hands'; (2) the trim budget was miscounted — 23 non-bj + 3 bj = 26 overflowed the history 25-cap and dropped the oldest entry, so the run-window count check failed 22-vs-23 on BOTH sides (a symmetric cap artifact, not a port bug) — after the trim, 21+3=24 with one entry of headroom).
- `tests/endpoint_test.js` identical **214 pass / 73 fail on both** (suite grew 282→284 with 2 new keno cases: csrf-gate 403 + anonymous 403; failing-set md5 aa3efbcb0142ce21b31adac6cac5a036 unchanged — no regression, only additions). Handler bodies verified live by the 187-check probe with minted sessions.
- Rate limits: unchanged — no RATE_LIMITS entry → `__default__ [100,60]`; anti-bot timing detector applies.

## Step 12 slots/vip-slots batch verification log (2026-09-15)

- `POST /api/casino/slots` + `/api/casino/vip/slots` ported into `routes/casino.rs` (one handler over an `is_vip_room` flag + dispatch + 3 new unit tests). **Slots** (server.js:23654-23721) — the first casino game that does NOT use readCasinoBet: the bet is the RAW `Number(body.amount)` (no toFixed(2) normalization — 2.005 stays 2.005 in the payout math); ladder order: casinoEnabled → tryParseJson → `isVip = stats[norm] && stats[norm].vip_casino_until > Date.now()` → VIP room without a pass → 403 `VIP pass required`; `isFreeSpin = (stats[norm]?.slots_free_spins || 0) > 0 && !isVipRoom` — ANY non-VIP slots play while free spins remain IS a free spin; `isVipRoom && bet < 100` → 400 `VIP minimum bet is 100 coins` (NaN < 100 is false, so NaN falls through to the isFinite ladders → `invalid bet`); free-spin ladder `!isFinite || <1 || >500` → 400 `invalid bet (free spins limit max 500 coins)`, then decrement `slots_free_spins` + saveUserStats BEFORE the settle (the settle's internal user_stats reload then includes the decrement, same as bun); paid ladder `!isFinite || <1 || > getCoins` → 400 `invalid bet`, non-VIP > 500 → the VIP-max string; 3 symbols from the same 7-symbol table; `slots_mult` extracted (triple → 💎100/7️⃣50/🔔20/🍒10/else 6, LEADING pair only → 3 — a trailing pair A,B,B pays nothing, else 0); dead rig re-draw while mult > 0 (≤20); `slots_rank` keyed on the PRE-bonus mult (mult===100 JACKPOT (Diamonds) / 50 Triple Sevens / 20 Triple Bells / 10 Triple Cherries / 6 Triple Fruit / 3 Double / else Lose; NaN compares unequal to every literal, same as JS); VIP room mult > 0 → ×1.1 through toFixed(2) AFTER the rank; payout = bet × mult (the RAW bet, un-normalized); settle with `(isFreeSpin, false)` opts — effectiveBet 0 for free spins; response `{ok, results, rank, mult, win, net, newBalance, freeSpinsRemaining}` — freeSpinsRemaining reads the LOCAL stats object (post-decrement in the fs path); **divergence recorded**: bun throws → 500 if the user has NO user_stats entry at all (`stats[norm].slots_free_spins || 0` on undefined), the port reads 0 — unreachable for any user who ever earned stats.
- Gates: fmt clean, clippy `-D warnings` clean (float match guards rejected as `redundant guard` — `slots_rank` uses literal patterns, NaN falls to `_` exactly like JS `===`); `cargo test --workspace` 71+11+78 green (3 new unit tests: `slots_mult_table` — all 7 symbols' triples incl. the 6x default + leading-pair-only (["💎","🍒","🍒"] → 0) + non-adjacent pairs; `slots_rank_map` — the 7 labels + NaN → 'Lose'; `slots_vip_boost_vectors` — 100→110, 6→6.6, 3→3.3 through toFixed(2)); `tests/parity_static.js` 102/102; **199-check live differential probe 0 fail** (~/probes/probe_casino.js: ladder +12 deterministic steps (slots/vip GET 405s, bad json ×2, slots no-amount/0/'abc' → `invalid bet`, slots 1e9 → VIP-max string, vip slots 50 → `VIP minimum bet is 100 coins`, vip slots 'abc'/'1e9' → `invalid bet` (NaN skips the min-bet check; 1e9 hits the balance cap), vip slots 100 → 403 `VIP pass required`); plays +4 (slots free 10 / free 2 / paid 1 / paid 2.005 with expectBet 2.005 RAW) −4 redundant (coinflip tails, dice over, crash 2.5, keno 1-8) keeping 21 non-bj + 3 bj settles = 24 under the history 25-cap; per-side logic — results = 3 legal symbols, mult === table(results), rank === the mult's label, freeSpinsRemaining === the tracked countdown; money math: free-spin plays settle effectiveBet 0 so expNet = expWin); **probe-design lessons recorded**: (1) reset_probe_env.js now seeds `slots_free_spins = 2` for the two playing users — with 5 seeded the first draft failed 8 checks because every non-VIP slots play (including the intended paid ones) IS a free spin while freeSpins > 0, so the paid path never ran; the fs plays now run first and exhaust the 2 spins, then the paid plays see freeSpinsRemaining 0; (2) the paid-path validation ladder steps run on user b (a NON-player), who has no free spins seeded — user b2/r2 would take the free-spin ladder instead.
- `tests/endpoint_test.js` identical **218 pass / 73 fail on both** (suite grew 284→288 with 4 new slots cases: slots + vip/slots csrf-gate 403s + anonymous 403s; failing-set md5 aa3efbcb0142ce21b31adac6cac5a036 unchanged — no regression, only additions). Handler bodies verified live by the 199-check probe with minted sessions.
- Rate limits: unchanged — no RATE_LIMITS entries → `__default__ [100,60]`; anti-bot timing detector applies.
