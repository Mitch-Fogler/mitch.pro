//! `/api/casino/*` — 15 games (plan Step 12). One game per commit, each with
//! its own state map + timers. Status: stub.

#![allow(dead_code)]

pub struct Casino;

impl Casino {
    pub fn placeholder() -> Self {
        Self
    }
}
