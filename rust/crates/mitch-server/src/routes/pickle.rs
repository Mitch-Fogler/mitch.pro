//! `/api/pickle-*`, `/api/pickle-club/*` — ~20 endpoints (plan Step 9).
//! Status: stub.

#![allow(dead_code)]

pub struct Pickle;

impl Pickle {
    pub fn placeholder() -> Self {
        Self
    }
}
